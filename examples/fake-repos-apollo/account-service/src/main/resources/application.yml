server:
  port: 8080
app:
  id: account-service
apollo:
  meta: http://apollo-prod.bank.internal:8080
  bootstrap:
    enabled: true
    namespaces: application,account.yaml,datasource.yaml
  cluster: default
