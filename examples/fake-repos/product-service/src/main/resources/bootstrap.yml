spring:
  application:
    name: product-service
  cloud:
    nacos:
      config:
        server-addr: nacos.internal:8848
        namespace: shop-prod-ns
        group: SHOP_PRODUCT
        data-id: product-service.yaml
        file-extension: yaml
