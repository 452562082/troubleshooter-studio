[project]
name = "python-service"
version = "1.0.0"
description = "Shop Python backend (FastAPI)"
requires-python = ">=3.11"

[project.dependencies]
fastapi = ">=0.104"
uvicorn = ">=0.24"
nacos-sdk-python = ">=0.1"
redis = ">=5.0"
