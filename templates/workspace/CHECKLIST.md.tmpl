# CHECKLIST.md - {{.Agent.Name}} 开工自检

排障前快速过一遍。缺项需要在首条技术回复里说明。

## 1. 环境

- [ ] 已确认目标环境：{{range $i, $e := .Environments}}{{if $i}} / {{end}}{{$e.ID}}{{end}}，或明确为全部环境
- [ ] 已确认实测域名

## 2. 代码基线

- [ ] 已执行 `git fetch --all --prune`
- [ ] 已通过 `routing` 确认目标分支
- [ ] 已切到目标分支并执行 `git pull --ff-only`
- [ ] 已记录 baseline commit hash
{{- $hasUmbrella := false -}}
{{- range .Repos -}}{{- if .ParentRepo -}}{{- $hasUmbrella = true -}}{{- end -}}{{- end }}
{{- if $hasUmbrella }}

本系统有 umbrella + 子模块关系。排障子模块时：

- [ ] 在 umbrella 仓执行 `git fetch --all && git pull --ff-only && git submodule update --init --recursive`
- [ ] 未单独在子模块目录 `git pull`
- [ ] 已记录 umbrella commit 和子模块 commit
{{- end }}

## 3. 取证链路

{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul")}}
- [ ] 已用 `routing` 解析配置标识
- [ ] 已按 `config-executor` 读取当前值、历史和 diff
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
- [ ] `resolve_runtime_static.py --env <env>` 能输出数据层连接串
{{- end}}
- [ ] 可观测性走可用 MCP 或明确 fallback
- [ ] 数据层走对应 skill：{{$first := true}}{{range .Infrastructure.DataStores}}{{if .Enabled}}{{if not $first}}、{{end}}{{$first = false}}{{dataStoreSkill .Type}}{{end}}{{end}}
- [ ] 不可用步骤已记录阻塞点和替代链路
{{- if hasSkill . "k8s-runtime-query" }}

## 4. K8s

- [ ] 已通过 `routing` 得到 cluster、namespace、label selector
- [ ] 已用 `k8s-runtime-query` 获取 pod 状态、events、current/previous logs
- [ ] `restartCount > 0` 时已查看 previous logs
- [ ] 需要 Deployment 状态时，已查看 `availableReplicas / readyReplicas`
{{- end }}

## 5. 配置与鉴权

- [ ] 已确认关键开关、路由、下游地址
- [ ] 涉及 host/IP/端口/连接串时，以 runtime 配置源为准，不只看代码默认值

{{- if .Infrastructure.Observability.Jaeger.Enabled}}
## 6. 链路追踪

- [ ] 已尝试从日志、响应头或用户反馈获取 trace ID
- [ ] 有 trace ID 时已展开 span 树
- [ ] 无 trace ID 时已按 service + 时间范围搜索

{{- end}}
## 7. 证据链

- [ ] 至少一种真实证据已就位：日志 / 实测请求 / 代码定位{{if .Infrastructure.Observability.Jaeger.Enabled}} / trace{{end}}
- [ ] 涉及接口返回时，已保留请求体和关键响应字段
- [ ] 数据/状态/计算/规则类问题已查代码和数据

## 8. 结论

- [ ] 已区分现象、根因、建议动作
- [ ] 结论可复现、可验证
- [ ] `confidence=low` 时未给改生产命令

## 9. 沉淀

`confidence=high` 必做：

- [ ] 故障快报最后含沉淀草稿 JSON：`pattern`、`typical_cause`、`next_actions`
- [ ] 已执行 `python3 skills/incident-investigator/scripts/sink_postmortem.py --input ...`
- [ ] 脚本退出码 0，stderr 显示 `[ok]` 或 `[skip] 已存在`
