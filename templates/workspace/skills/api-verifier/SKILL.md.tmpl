---
name: api-verifier
description: 验证 API、webhook、定时任务、回调或纯后端 Bug 时使用。通过重放请求、对比响应证据或验证修复结果完成取证，不做根因分析。
---

# API 验证

## 使用场景

当验证入口来自下面这些信息时使用本 skill：

- API endpoint、webhook、callback、scheduled job 或队列触发动作。
- 可重放的 curl / HAR 请求。
- 预期响应、状态码、响应体或数据状态与实际不一致。
- 修复后需要用同一请求或同一任务输入做回归验证。

本 skill 只收集请求和响应证据，不做根因分析，不输出修复建议。

## 必要输入

缺信息时一次只问一个最小阻塞项：

- env。
- method + URL / route。
- 复现所需 headers；密钥必须脱敏，或通过本地环境变量提供。
- request body、query string、entity id 或 job 参数。
- 预期 status / body / state。
- 实际 status / body / state。
- 如果查历史证据，需要时间窗口、request id 或 trace id。

## 重放流程

1. 读取 `routing/SKILL.md`，确认 env、域名、服务和鉴权方式。
2. 构造重放命令，展示输出时必须隐藏密钥。
3. 优先使用安全的只读请求。变更类请求必须获得用户明确许可，并使用测试实体或测试环境。
4. 采集以下证据：
   - request method 和归一化 URL。
   - response status。
   - response body 摘要，不贴完整敏感 payload。
   - 携带 request id、trace id、error code 的响应头。
   - elapsed time。
5. 如果有 request id 或 trace id，只用 `tracing-query` 或日志查询 skill 确认同一个请求发生了什么。
6. 对比实际响应和可见副作用是否符合预期。
7. 返回验证状态，不推断实现层原因。

## 命令模板

HTTP 重放优先使用下面的形态：

```bash
curl -sS -D /tmp/tshoot-api-headers.txt \
  -o /tmp/tshoot-api-body.json \
  -w 'status=%{http_code} time_total=%{time_total}\n' \
  -X <METHOD> '<URL>' \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <redacted-or-env-token>' \
  --data '<json-body-if-needed>'
```

然后只摘要输出，不粘贴密钥或超大 payload：

```bash
head -80 /tmp/tshoot-api-headers.txt
python3 - <<'PY'
import json
from pathlib import Path
p = Path('/tmp/tshoot-api-body.json')
try:
    data = json.loads(p.read_text())
    print(json.dumps(data, ensure_ascii=False, indent=2)[:4000])
except Exception:
    print(p.read_text(errors='replace')[:4000])
PY
```

## 证据规则

- 必须脱敏 `Authorization`、`Cookie`、`Set-Cookie`、token、password、signature、手机号、邮箱和个人标识。
- 结论必须绑定具体 request、response、timestamp、env 和可用的 build/version。
- 变更类 API 必须记录测试实体 id 和清理预期。
- 如果请求不能安全重放，使用 `insufficient_info`，并列出缺失的安全重放条件。

## 输出契约

```yaml
api_verification:
  verification_status: "reproduced|not_reproduced|insufficient_info|fixed_verified|still_reproduces"
  environment: "<env>"
  request:
    method: "<method>"
    url_or_route: "<url-or-route>"
    entity_id: "<id-or-empty>"
  response:
    status: "<http-status-or-empty>"
    body_summary: "<summary>"
    elapsed_ms: "<elapsed-or-empty>"
  correlation:
    request_ids: []
    trace_ids: []
  expected_behavior: "<expected>"
  observed_behavior: "<observed>"
  evidence:
    - "<artifact-or-command-summary>"
  gaps:
    - "<missing-input-or-unchecked-scope>"
```

## 硬约束

- 没有用户明确许可时，不对生产环境执行变更类请求。
- 不暴露密钥或完整敏感 payload。
- 不输出根因判断或修复建议。
