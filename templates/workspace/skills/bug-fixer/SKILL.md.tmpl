---
name: bug-fixer
description: Bug 修复 Agent 统一入口。用于基于已验证证据和排障结论，从用户确认的开发基线创建修复分支，完成最小改动、测试、提交和推送。
---

# Bug 修复

## 使用场景

当用户在 Bug 工单里明确点击或要求“修复”时使用本 skill。

只有排障结果同时满足 `root_cause_type: code` 与 `remediation.mode: code_change` 时才能进入本 skill。`data`、`configuration`、`infrastructure`、`network`、`external_dependency` 和 `transient` 根因必须返回 Studio 的非代码处置流程；不得创建空修复分支、伪造 commit，也不得用代码改动替代数据库修正、配置发布、运行时恢复或外部依赖恢复。

本 skill 不负责首次复现，也不负责只读根因分析。它接收验证 Agent 的证据和排障 Agent 的结论，执行代码修复闭环：

- 确认用户批准的开发基线和目标环境分支。
- 从 Studio 锁定的开发基线创建修复分支。
- 根据证据和根因做最小必要改动。
- 运行相关测试或构建。
- 提交并推送修复分支。
- 返回修复分支，等待 Studio 单独授权后分别合入开发基线和环境分支。

## 必要输入

开始前必须已经有：

- Bug ID、标题、环境和复现/验证证据。
- 排障 Agent 输出的根因结论、受影响服务/模块/仓库。
- 结构化根因必须是 `root_cause_type: code` 且 `remediation.mode: code_change`。
- 用户在修复授权中批准的仓库 → 开发基线映射。
- 环境分支映射，来自 `routing/references/env-branch-map.yaml`。
- 本地仓库路径，来自部署时保存的 repo path 或工单上下文。

缺少阻塞输入时停止，不要猜。

## 分支规则

1. Studio 同时提供用户批准的开发基线和 `env-branch-map.yaml` 确定的目标环境分支；两者允许不同。当前仓库 checkout 不能替代任何一个绑定。
2. 只能在 Studio 提供的“locked fix workspaces”中修改，并从其中锁定的开发基线 commit 创建修复分支。缺少宿主 worktree、仓库或基线绑定时停止并报告配置缺口；不得自行从当前 HEAD 或环境分支兜底。
3. 创建修复分支前运行：

```bash
git status --short
git branch --show-current
```

4. 如果存在未提交改动：
   - 能明确判断是本次修复 Agent 刚产生的改动，继续。
   - 否则停止并提示用户处理，不要覆盖或顺手提交用户改动。
5. 修复分支命名：

```text
fix/bug-<source>-<id>-<short-slug>
```

示例：`fix/bug-zentao-909-category-count`。

6. 在 Studio 专用 worktree 中验证并从锁定开发基线创建分支：

```bash
test "$(git rev-parse HEAD)" = "<locked-source-baseline-commit>"
git switch -c fix/bug-<source>-<id>-<short-slug>
test "$(git merge-base <locked-source-baseline-commit> HEAD)" = "<locked-source-baseline-commit>"
```

上面的 `git switch -c` 只允许在 Studio 已 detached 到锁定 commit 的专用 worktree 中执行。不得在用户 checkout 中使用不带起点的 `git checkout -b` / `git switch -c`。分支已存在时，先确认它是当前 Bug 的修复分支且锁定基线祖先正确；不能确认时停止。

## 修复流程

1. 读取上一轮验证证据和排障结论，提取：
   - 失败入口。
   - 实际行为和期望行为。
   - 相关接口、页面、服务、表或配置。
   - 根因定位文件或模块。
2. 用 `routing` 确认环境、仓库和分支；必要时读取 `repo-stack-map.yaml` 和 `frontend-entry-map.yaml`。
3. 定位代码时优先用结构化搜索：
   - Go：`rg`、`go test ./...`、包级测试。
   - Node/TS：`rg`、`npm test`、`npm run build`、相关 lint/typecheck。
   - Java：`mvn test`、`gradle test`、模块级测试。
4. 做最小必要改动：
   - 不做无关重构。
   - 不改格式化无关大文件。
   - 不修改生成物、lockfile 或配置，除非根因明确要求。
   - 不删除用户已有逻辑，除非有证据证明它就是错误点。
5. 运行最小闭环测试：
   - 先跑直接相关测试。
   - 风险较高或共享模块改动时跑更大范围测试。
   - 如果测试无法运行，记录命令、失败原因和残余风险。
6. 提交前复查：

```bash
git diff --check
git diff --stat
git status --short
```

7. 提交：

```bash
git add <files>
git commit -m "fix: <short bug fix summary>"
```

8. 推送：

```bash
git push -u origin <fix-branch>
```

修复 Agent 到此为止，不自行合并开发基线或环境分支。用户在 Studio 单独确认合并后，Studio 会以修复分支为合并源，分别推进已确认的开发基线和目标环境分支；两者相同时只执行一次。

### Studio durable checkpoint

Studio phase prompt 提供 `STUDIO_EVIDENCE_STAGING_DIR`、Case ID 和 attempt ID 时，修复 Agent 必须维护 `${STUDIO_EVIDENCE_STAGING_DIR}/fix-checkpoint.json`：

1. **第一次 push 前**，先写 `state: prepared`。其中 `result` 是计划完成后将返回的完整 `FixResult`，必须覆盖所有仓库的 base/fix/target branch、完整 commit、push remote 和对应 commit 的测试证据。
2. 写入使用同目录临时文件，flush/fsync 文件后原子 rename 为 `fix-checkpoint.json`；不得直接原地覆盖半个 JSON，也不得写入 token、SSH key、Cookie 或其它凭据。
3. 然后逐仓执行精确 fix branch push。全部 push 成功后，以相同原子流程把同一 manifest 更新为 `state: pushed`，再输出最终 YAML。
4. JSON 顶层固定为：`kind: studio_fix_checkpoint`、`version: 1`、prompt 给出的 `case_id`、`attempt_id`、`state: prepared|pushed`、`result`。

`prepared` 不表示已经推送。正常完成和恢复都会通过 SSH remote 精确检查每个 fix branch 是否指向 manifest commit；只有本地 commit 绝不会被当成已推送。临时 DNS/SSH/权限不可用时 Studio 会保留 checkpoint 并有界重查，远端可读后的 ref mismatch 或少一个仓库才失败关闭。不要为了恢复而重复提交、强推或改写 manifest scope。

## 多仓库规则

如果一个 Bug 需要改多个仓库：

- 逐仓库确认开发基线和目标环境分支。
- 每个仓库各自创建同名或同前缀修复分支。
- 每个仓库独立提交和推送。
- 最终输出列出每个仓库的分支、commit 和测试结果。

如果只确认一个仓库需要改，不要顺手改其它仓库。

## 禁止行为

- 不自行部署。
- 不直接改生产配置。
- 不执行 `git reset --hard`、`git clean -fd`、强推、删除分支等破坏性命令。
- 不在用户原始 checkout 中切分支、合并或修复；Studio 提供专用 worktree 时原 checkout 只读。
- 不把其它功能分支或环境分支 merge/rebase 到修复分支；修复提交必须是锁定开发基线 SHA 的线性后代。
- 不提交用户已有未提交改动。
- 不把 token、cookie、password、authorization header 写入代码、日志或提交信息。
- 不凭感觉扩大修复范围。

## 输出契约

修复完成或阻塞时输出：

```yaml
fix_status: fixed_pushed | blocked | failed
environment: "<env>"
branches:
  - repo: "<repo>"
    base_branch: "<user-approved-source-baseline-branch>"
    fix_branch: "<fix-branch>"
    commit: "<sha-or-empty>"
    pushed: true
    target_environment_branch: "<env-branch>"
    push_remote: "<remote>"
changes:
  - repo: "<repo>"
    summary: "<file-or-module>: <what changed>"
tests:
  - repo: "<repo>"
    commit: "<tested-fix-commit>"
    command: "<command>"
    result: passed | failed | skipped
    note: "<short evidence>"
    skipped_reason: "<required only when result=skipped>"
deployment_notice: "修复分支已推送；等待 Studio 将修复提交合入开发基线和环境分支，再部署环境分支并触发回归。"
risks:
  - "<remaining-risk-or-empty>"
blocked_reason: "<only when blocked/failed>"
evidence: []
```

每个仓库的 `base_branch` 必须是 Studio 锁定的用户确认开发基线；`target_environment_branch` 是后续独立集成目标，两者允许不同。`fix_branch` 必须是不同的专用修复分支，禁止直接在基线分支或环境分支修复、提交或推送。

## 完成标准

只有同时满足以下条件才返回 `fixed_pushed`：

- 已从 Studio 锁定的用户批准开发基线创建修复分支。
- 代码已最小改动修复。
- 已运行相关测试或明确说明无法运行原因。
- 已提交。
- 已推送。
- 已说明等待 Studio 的基线/环境分支合并授权和后续环境部署。
