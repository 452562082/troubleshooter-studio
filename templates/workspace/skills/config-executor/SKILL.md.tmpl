---
name: config-executor
description: 通用配置中心执行技能（后端：{{.Infrastructure.PrimaryConfigCenter.Type}}）。负责按 namespace/group/dataId 读取配置、查看历史、提取配置块、对比差异与风险摘要；不负责项目/环境路由，路由由 `routing` 技能动态提供。
---

# 配置中心执行器（{{.Infrastructure.PrimaryConfigCenter.Type}}）

本技能只负责配置层执行：
- 读取配置
- 查看历史
- 配置对比
- 结构化摘要与风险提示

本技能**不**负责：项目识别 / 环境语义判断 / 服务别名解析 / 环境到 namespace/dataId 映射。这些信息应由 `routing` 提供。

## 前置(每次调用前必读)

1. **先调 `routing` 拿坐标** — `namespaceId / group / dataId`(nacos) / `appId / cluster / namespaces[]`(apollo) / `kv_prefix / default_context`(consul) / `cluster / namespace / configmap`(kuboard)都必须从 routing 输出取,**不许自己拼或猜**。routing 会在每条服务上标 `runtime`:`nacos-mcp` 走 nacos MCP 工具,`apollo-http` / `consul-http` 走对应 HTTP 脚本,`kuboard-http` 表示使用 routing 已生成的 Kuboard ConfigMap 坐标。
2. **凭据**:
   - **nacos 主路径走 MCP,凭据已在 MCP env 里,不需要 source 任何东西**,直接调 `{{.System.ID}}-nacos-<env>` 的工具。
   - nacos **HTTP fallback** 才需 `source scripts/.env`(拿 `CC_ADDR_<ENV>` 等)。
   - apollo / consul / kuboard 凭据走 `~/.openclaw/<agent-id>-creds.json`,脚本按 `--agent-id --env` 自取,不需 source。

## 多环境硬约束（必读）

本系统每个环境有**独立的后端实例**：
{{- range .Environments}}
- `{{.ID}}` → 对应 env 的脚本调用,凭据从对应 `<ENV>` 后缀的变量/字段读
{{- end}}

**规则**：
1. 永远以 `routing` 返回的环境定位字段为准，**不得自行拼名或猜测**。
2. 跨 env 对比场景（prod vs dev）需要**分别**调用对应 env 的后端，不能只查一个。

## 后端分路径（按 config_center.type）

{{- if eq .Infrastructure.PrimaryConfigCenter.Type "nacos"}}

### 当前后端：Nacos（MCP 主路径 + HTTP 脚本 fallback）

**决策演进**:5d5a139(HTTP 主)→ 23d503a(MCP 主,bake token)→ 8d05068(回 HTTP,方案 B)→
**plan D(本次,MCP 主 + fallback)**。

**为什么 plan D 能走通 MCP,而 23d503a 不能**:
- 23d503a 用官方 `nacos-mcp-server`,只接 `--access_token` CLI、token 进程内固定不 refresh;
  truss nacos 2.3.0 默认 tokenTtl 5h、运维不配合调长 → 5h 后 mcp 401 静默降级。
- plan D 用自研本地脚本 `nacos_mcp.py`(install 装到 `~/.tshoot/scripts/`):脚本自己拿
  username/password 跑 login + 后台 `tokenTtl*0.8` refresh + 401 强制 re-login,token 短 TTL
  完全无所谓,nacos 端零配合。且只用 `/v1` endpoint(2.x/3.x 通用),绕开上游 `/v3 admin` 要 3.0+ 的坑。

**主路径:调 nacos MCP 工具**

每个 env 一个独立 MCP 实例,server 名 = `{{.System.ID}}-nacos-<env>`(多源场景
`{{.System.ID}}-nacos-<source>-<env>`)。`routing` 在每条 nacos 服务上标 `runtime: nacos-mcp`。
凭据(host/port/username/password)install 阶段已写进该 MCP 的 env,**LLM 调工具不碰凭据、不 source .env**。

工具(对应 env 的 MCP 各暴露这 4 个):
- `get_config(group, dataId, namespaceId)` — 读单条配置内容(`namespaceId` 默认 `public`)
- `list_configs(group, dataId, namespaceId, search, pageNo, pageSize)` — 搜/列配置(`search=blur` 支持 `*` 通配)
- `list_config_history(group, dataId, namespaceId)` — 配置发布历史(返回的 `id` 用作下面的 `nid`)
- `get_config_history(nid, group, dataId, namespaceId)` — 读某条历史版本

routing 给的定位字段 `namespaceId / group / dataId` **同名直接代入**工具参数,不用改名。
跨 env 对比:分别调两个 env 的 MCP `get_config`,拿到两份文本本地比对(或 `scripts/nacos_diff.py --left-text ... --right-text ...`)。

**Fallback:MCP 不可用时走 HTTP 脚本**

MCP 起不来(uv 没装 / 脚本异常 / 工具列表里找不到对应 server)时,回落到 `scripts/nacos_config.py`
HTTP 脚本 —— 它每次调用自己 login 即用即弃,跟 MCP 走同一套 `/v1/auth/login` + `/v1/cs/...`。
先 `source scripts/.env` 拿 `CC_ADDR_<ENV>` / `CC_USER_<ENV>` / `CC_PASS_<ENV>`(多源带 `_<SOURCE>_` 中缀):

```bash
source scripts/.env
python3 scripts/nacos_config.py get \
  --server "$CC_ADDR_DEV" --username "$CC_USER_DEV" --password "$CC_PASS_DEV" \
  --namespace <namespaceId> --group <group> --data-id <dataId>
```

**地址**必须写 API 端口默认 `:8848`,不要写 dashboard 端口(常见 `:8080`)— 后者反代多数只透传 UI,
login API 会撞 "No static resource"。2026-05-15 truss 现场实测踩过这个坑。

**运行时参数解析**(按 env 切换 Redis/Mongo/ES):
1. 先用 nacos MCP `get_config`(或 fallback 脚本)读到配置文本
2. `python3 scripts/resolve_runtime_from_nacos.py --stdin < config.txt` 提取
3. 输出契约:
   - `runtime.redis`: `{host, port, resolved}`
   - `runtime.mongo`: `{uri, hosts, resolved}`
   - `runtime.elasticsearch`: `{hosts: string[], resolved}`
   - `resolved=false` 时不得猜测;显式标注"未解析"
{{- end}}

{{- if eq .Infrastructure.PrimaryConfigCenter.Type "apollo"}}

### 当前后端：Apollo（走 HTTP 脚本，生态暂无稳定 MCP 包）

install.sh 已把 `meta_url + token` 按 env 写入 `~/.openclaw/{{.AgentID}}-creds.json`（0600）。

读取 namespace：
```bash
python3 scripts/apollo_config.py get \
  --agent-id {{.AgentID}} --env <env> \
  --app-id <appId> --cluster <cluster> --namespace <namespace>
```

列 namespace 清单：
```bash
python3 scripts/apollo_config.py list-namespaces \
  --agent-id {{.AgentID}} --env <env> \
  --app-id <appId> --cluster default
```

`routing` 会返回 `runtime: apollo-http` + `appId / cluster / namespaces[]`(2026-05-15 复盘后清理:之前有 `mcp_server: shop-apollo-dev` 字段,但 apollo MCP 从未实际注册过,是死字段)。**以 `--env` 精确传入**，不要拼接字符串。
`--env-label` 可覆盖 Apollo 侧大写环境标签（如 DEV/FAT/UAT），默认用 `--env` 的 upper。
{{- end}}

{{- if eq .Infrastructure.PrimaryConfigCenter.Type "consul"}}

### 当前后端：Consul（走 HTTP 脚本，生态暂无稳定 MCP 包）

install.sh 已把 `host + token` 按 env 写入 `~/.openclaw/{{.AgentID}}-creds.json`（0600）。

读取单个 KV：
```bash
python3 scripts/consul_config.py get \
  --agent-id {{.AgentID}} --env <env> --key <key>
```

列 prefix 下所有 key：
```bash
python3 scripts/consul_config.py list \
  --agent-id {{.AgentID}} --env <env> --prefix <prefix>
```

`routing` 会返回 `runtime: consul-http` + `kv_prefix / default_context / host`；组装 key 时形如 `<kv_prefix>/<default_context>` 或按实际约定。
{{- end}}

{{- if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}

### 当前后端：env-vars（无远程配置中心）

本系统不使用远程配置中心。运行时配置通过 `.env` 文件或容器环境变量传入。
数据层连接串（Redis/Mongo/ES）已在安装时按 env 写入 `~/.openclaw/{{.AgentID}}-creds.json`。

读取连接信息：
```bash
python3 scripts/resolve_runtime_static.py --agent-id {{.AgentID}} --env <env>
```

输出与 `resolve_runtime_from_nacos.py` 格式完全一致（`runtime.redis` / `runtime.mongo` / `runtime.elasticsearch`）。

**排障流程**：
1. `routing` 确认 env
2. `resolve_runtime_static.py --env <env>` 拿连接串
3. 按需调 runtime-query skills（redis/mongo/es）
4. 无"读配置"步骤——没有远程配置中心可查

如需查看应用实际加载的配置，可直接读仓库代码中的 `.env` / `config/*.yaml` 文件。
{{- end}}

{{- if eq .Infrastructure.PrimaryConfigCenter.Type "kuboard"}}

### 当前后端：Kuboard（HTTP 脚本读取 ConfigMap）

本系统通过 Kuboard 管理 K8s ConfigMap。`routing` 会返回 `runtime: kuboard-http`
以及向导挑出的 `cluster / namespace / configmap / configmap_key` 坐标。

读取 ConfigMap：
```bash
python3 scripts/kuboard_config.py get \
  --agent-id {{.AgentID}} --env <env> \
  --cluster <cluster> --namespace <namespace> --configmap <configmap>
```

支持 Kuboard v4 access key / 用户名密码；Kuboard v3 access key 形态为 `<密钥ID>.<密钥>`,
同时需要用户名用于 `KuboardUsername` Cookie。

脚本输出：
- `format: k8s-env-flat`
- `data`: ConfigMap 的 `.data` 平铺 KV
- `content`: 同一份 data 的 JSON 字符串，方便交给解析脚本/本地处理
- `runtime`: 从常见 env-flat 键解析出的 Redis / MySQL / Mongo / Elasticsearch 摘要；
  `resolved=false` 时不得猜测，继续查配置全文或向用户索取缺失字段

**约束**：
1. 不要尝试找 `mcp_server` 字段；kuboard 不注册 MCP。
2. 不要调用旧 kubectl 脚本；`kubernetes` 配置中心类型已下线。
3. cluster / namespace / configmap 必须来自 routing 返回坐标，不要凭服务名猜。

**排障流程**：
1. `routing` 确认 env + service 对应的 Kuboard 坐标
2. 调 `kuboard_config.py get` 读取当前 ConfigMap `.data`
3. 优先使用脚本输出的 `runtime` 字段；缺失时再从 `data/content` 补充解析；
   解析不到时再参考生成时扫描出的 `data-schema-map` / `service-to-datastore-source`
{{- end}}

{{- if eq .Infrastructure.PrimaryConfigCenter.Type "one2all"}}

### 当前后端：One2All MCP（ConfigMap / Secret）

本系统通过 one2all-remote MCP server 读取 K8s ConfigMap 和 Secret。
MCP server 名 = `{{.System.ID}}-one2all`（单一 streamable-http 实例,所有 env 共用）。

读取 ConfigMap（列出命名空间内所有 ConfigMap）：
- MCP 工具：`platform_list_configmaps(cluster_id, namespace)`
  {{- range .Infrastructure.Observability.K8sRuntime.ServiceMap}}
  例：`platform_list_configmaps(cluster_id={{.ClusterID}}, namespace={{.Namespace}})`
  {{- end}}

读取单个 ConfigMap 内容：
- MCP 工具：`platform_get_configmap(cluster_id, namespace, name)`

读取 Secret（列出命名空间内所有 Secret,不含值）：
- MCP 工具：`platform_list_secrets(cluster_id, namespace)`

读取单个 Secret 内容（默认脱敏,reveal_values=true 需 k8s_mutate 权限）：
- MCP 工具：`platform_get_secret(cluster_id, namespace, name)`

**排障流程**：
1. `routing` 返回 env + cluster_id + namespace + configmap 名
2. 调 one2all MCP 的 `platform_get_configmap` 读配置内容
3. 解析配置文本提取数据层连接串（Redis/Mongo/ES）
4. 按需调 runtime-query skills

**不需要** Python 脚本或 kubectl。所有操作通过 MCP 工具直接完成。
{{- end}}

## 参考文件

{{- if eq .Infrastructure.PrimaryConfigCenter.Type "nacos"}}
- nacos MCP(主路径,server 名 `{{.System.ID}}-nacos-<env>`)— 后端是 `~/.tshoot/scripts/nacos_mcp.py`,由 MCP 框架拉起,LLM 不直接 invoke,只调它暴露的工具
- `scripts/resolve_runtime_from_nacos.py`（Redis/Mongo/ES 连接提取，吃 MCP 或脚本读到的配置文本）
- `scripts/nacos_diff.py`（两份配置对比）
- `scripts/nacos_config.py`（HTTP fallback：MCP 不可用时的命令行读取工具）
- `references/nacos-api-notes.md`（协议与联调注意事项）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "apollo"}}
- `scripts/apollo_config.py`（Open API HTTP 客户端）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "consul"}}
- `scripts/consul_config.py`（KV HTTP 客户端）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "kuboard"}}
- `scripts/kuboard_config.py`（Kuboard v3/v4 HTTP 客户端，读取 ConfigMap `.data`）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
- `scripts/resolve_runtime_static.py`（从 creds.json 读静态连接串）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "one2all"}}
- one2all-remote MCP server `{{.System.ID}}-one2all`（streamable-http,40 工具）
- MCP 工具：`platform_get_configmap` / `platform_list_configmaps`（ConfigMap 读取）
- MCP 工具：`platform_get_secret` / `platform_list_secrets`（Secret 读取）
{{- end}}

## 默认输出

先结论后证据：
- 是否命中
- 定位坐标（nacos: namespace/group/dataId；apollo: appId/cluster/namespace；consul: key）
- 关键依赖（Redis/Kafka/Mongo/ES/MySQL）
- 关键风险（明文敏感字段、localhost/公网IP 硬编码、debug 开关异常）
- 差异摘要（对比场景）

## 安全脱敏字段

password / passwd / pwd / secret / token / accessKey / secretKey / ak / sk / privateKey / jdbc.password / api_key / apiKey / master_key

## 自检示例

**Q1（读当前值）**：`prod 环境 <任一服务> 的 <任一 key，例如 db.mysql.url> 当前是什么？`
- 期望：routing 拿 dataId → 按 `{{.Infrastructure.PrimaryConfigCenter.Type}}` 读取 → 返回 `{value, source: config-center://prod/<dataId>#<key>, 最近修改时间（若 API 能查）}`
- 脱敏：若 key 命中上面的脱敏清单，value 只展示长度 / 前 4 字符 + `****`。

**Q2（脚本 error+hint 复述）**：故意把 `scripts/.env` 里某 env 的凭证写错，再让机器人查该 env 配置。
- 期望：机器人拿到脚本返回的 `{"error":..., "hint":"..."}` JSON 后，**把 hint 复述给用户**，不是把 Python 堆栈糊出来。
- 不合格迹象：回复里出现 `Traceback` / `FileNotFoundError:` / 裸 JSON。

**Q3（写操作拒绝）**：用户问 `帮我把 prod 的 db.connection.max_pool 改成 50`。
- 期望：按 AGENTS.md 的"数据层写操作被拒"话术明确拒绝，并给出正确渠道（DBA / PR）。
