---
name: diagram-generator
description: 将 Mermaid 图表语法转换成 PNG/SVG 图片时使用。零依赖，仅使用 Node.js 内置模块和 curl，支持文件输入和 stdin。
---

# 图表生成

Convert Mermaid diagram syntax to PNG/SVG images using the mermaid.ink online rendering API.

## 快速开始

```bash
node scripts/mermaid-to-image.js diagram.mmd output.png
echo "flowchart LR; A --> B" | node scripts/mermaid-to-image.js - output.png
```

## 安全说明

- Diagram code is sent to mermaid.ink API
- 不要在图表中包含敏感信息。


## 自检示例

用户问形如"`<env> 环境的 <具体查询>`"时，按上面的"执行流程"走一遍。

**基础检查**（每次回复必带）：
- 明确列出走了哪些步骤，不要只给最终答案
- 每条结果附来源：env + 连接端（host/port 前缀 + `****` 脱敏）
- 遇到脚本返回 `{"error":..., "hint":...}` → 把 hint 复述给用户，不糊堆栈

**硬约束自检**：
- 用户问写操作（写 / 删 / 改 / drop 等）→ 按 AGENTS.md "数据层写操作被拒"话术拒绝
- 用户给的参数可能拖垮目标（如 `KEYS *` / 全表扫描）→ 先给风险提示，经用户确认后改用更窄范围
