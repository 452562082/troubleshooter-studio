---
name: elk-log-query
description: 通过 ELK（Elasticsearch + Kibana）查询日志。按 service、时间、关键词、trace ID 搜索日志，作为 Loki 的替代方案（二选一或共存）。
---

# ELK Log Query

## 何时使用

- 系统使用 ELK（Elasticsearch + Logstash/Filebeat + Kibana）而非 Loki 做日志
- 需要按关键词 / trace ID / error level / 时间范围搜索日志
- 需要统计错误数量趋势

## 执行流程

1. 确认 env（通过 `routing`）。
2. 获取 Kibana/ES URL：
{{- range .Environments}}
   - `{{.ID}}` → config 中声明的 Kibana 或 ES URL
{{- end}}
3. 构造查询：
   - 简单搜索 → Kibana Discover 或 ES `_search` API
   - 精确搜索 → ES DSL（bool query + range + match）
4. 分析结果：错误日志聚合 → 时间线 → 关键异常栈。

## Elasticsearch API 查询

### 按关键词 + 时间范围搜索

```bash
curl -X POST "$ES_URL/{{if .Infrastructure.Observability.ELK.DefaultIndex}}{{.Infrastructure.Observability.ELK.DefaultIndex}}{{else}}app-logs-*{{end}}/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"match": {"message": "<keyword>"}},
          {"match": {"service": "<service>"}},
          {"range": {"@timestamp": {"gte": "now-1h", "lte": "now"}}}
        ]
      }
    },
    "sort": [{"@timestamp": "desc"}],
    "size": 20
  }'
```

### 按 trace ID 精确查

```bash
curl -X POST "$ES_URL/{{if .Infrastructure.Observability.ELK.DefaultIndex}}{{.Infrastructure.Observability.ELK.DefaultIndex}}{{else}}app-logs-*{{end}}/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {"match": {"trace_id": "<traceID>"}},
    "sort": [{"@timestamp": "asc"}],
    "size": 100
  }'
```

### 错误日志聚合（最近 1 小时 TOP 错误）

```bash
curl -X POST "$ES_URL/{{if .Infrastructure.Observability.ELK.DefaultIndex}}{{.Infrastructure.Observability.ELK.DefaultIndex}}{{else}}app-logs-*{{end}}/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"match": {"level": "ERROR"}},
          {"range": {"@timestamp": {"gte": "now-1h"}}}
        ]
      }
    },
    "aggs": {
      "top_errors": {
        "terms": {"field": "message.keyword", "size": 10}
      }
    },
    "size": 0
  }'
```

### Kibana 快捷方式

如果有 Kibana URL，也可直接构造 Discover 链接让用户在浏览器中查看：
```
$KIBANA_URL/app/discover#/?_a=(query:(language:lucene,query:'service:<svc> AND level:ERROR'))
```

## 输出要求

- 先结论：是否发现异常日志
- 再证据：
  - 搜索条件（index / query / time range）
  - 命中条数
  - TOP 错误消息（出现次数 + 样例）
  - 关键日志行（时间 + trace_id + 消息摘要）
- 最后归因：代码异常 / 外部依赖超时 / 配置错误

## 硬约束

- 仅只读查询（`_search` / `_count`）；禁止写操作（`_delete` / `_bulk`）。
- 必须先通过 `routing` 确认 env，再用对应 env 的 ES/Kibana URL。
- 日志内容可能含敏感信息，输出时对 token/password/身份证号做脱敏。

## 自检示例

**Q1（按 trace_id 搜日志）**：`dev 的 order-service 最近 1 小时 trace_id=abc123 的日志`
- 期望：routing 拿 log-app → ES `_search`，query `trace_id: "abc123" AND service: "order-service"` + `range` 最近 1h → 返回按时间排序的日志，附时间戳、level、message 摘要。

**Q2（错误关键字搜索）**：`prod 最近 10 分钟 xxx 服务有哪些 ERROR？`
- 期望：query `level: ERROR AND service: "xxx"` + 时间窗 10m → 按错误聚类（用 `logger` / 前 80 字符 hash 分组），返回 Top 3 错误模板 + 次数。

**Q3（时间窗不对 / 无结果）**：用户问"30 秒前挂的，查日志"但默认查 1h 就可能空。
- 期望：按 AGENTS.md "Grafana/Loki/Jaeger/ELK 查询返回空或 403" 段处置——提示时间窗可能过粗，建议 `@timestamp:["now-2m" TO "now"]`，并让用户确认时区（UTC vs CST）。

**Q4（403 / 凭证失效）**：脚本返回 `{"error":"403 Forbidden", "hint":"..."}`。
- 期望：复述 hint，不糊堆栈。
