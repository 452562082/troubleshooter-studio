---
name: frontend-repro-investigator
description: 从前端复现证据开始排障。读取 HAR、console、RUM/Sentry 链接或用户复现步骤，提取失败请求、trace/request id、静态资源问题和后端候选接口，再交给 tracing/log/runtime skills 深挖。
---

# 前端复现取证

## 使用场景

当报告从前端现象开始时，先使用本 skill，再进入纯后端排查：

- 页面白屏、按钮无响应、上传失败、渲染不一致。
- 浏览器或设备特定问题。
- console error、Sentry/RUM/ARMS 链接、HAR 文件。
- 测试人员说“我在前端复现了”。

不要一开始就查后端 trace。先确认浏览器实际看到了什么。

如果由 `bug-verifier` 调用，归一化证据和 `backend_handoff` 字段后就停止，不继续做根因分析或原因排序。

## 必要输入

缺信息时一次只问一个最小阻塞项：

- env。
- 页面 URL 或 route。
- 精确交互步骤。
- 发生时间和时区。
- 至少一个证据：HAR 文件、console 文本/截图、RUM/Sentry 链接、trace id 或 request id。

## 证据优先级

优先使用能生成后端交接字段的结构化证据：

1. Sentry / RUM event id，且包含 `trace_id`、request id、API URL 或 JS stack。
2. HAR 或浏览器采集输出，包含失败/慢请求、trace header、静态资源状态、CSP header 或 mixed-content/TLS block。
3. console 文本/截图，包含 API status、ChunkLoadError、CSP violation、mixed-content/TLS message、source-map reference 或 JS stack。
4. 只有手工复现步骤时，先索要上面最小缺失证据。

优先使用 `backend_handoff.trace_ids`。没有 trace id 时，用 `backend_handoff.candidate_endpoints` 结合 `routing/references/frontend-entry-map.yaml` 选择候选后端服务，再查日志。

## Studio Case 与手动兼容浏览器采集

Studio 故障闭环中的 Web validation / regression 由 **Studio BrowserVerifier** 在宿主进程执行 BrowserPlan，生成渲染截图、脱敏 Network / console 和 action trace，并把精确 artifact 引用注入 validator。此时本 skill 只归一化 Studio 已冻结的证据，不得运行本目录的采集器替代宿主结果。

`browser_collect.mjs` 只是脱离 Studio Case 时的**手动兼容路径**，不属于持久化 Case 证据协议：它不具备 HostVerifier 的 URL/生产动作策略、加密登录 session、attempt staging journal、artifact 冻结与截图成功 gate，产物不能冒充 Studio 的权威浏览器证据。只有用户明确要求手动兼容采集、提供可复现 URL，且生成工作区有 Node.js 时，才先展示采集计划，不立即启动浏览器：

```bash
node skills/frontend-repro-investigator/scripts/browser_collect.mjs --url <page-url> --out /tmp/tshoot-artifacts --plan
```

确认适合做浏览器自动化后再运行：

```bash
node skills/frontend-repro-investigator/scripts/browser_collect.mjs --url <page-url> --out /tmp/tshoot-artifacts
```

采集器会写出：

- `network.har`
- `console.jsonl`
- `screenshot.png`
- `trace.zip`

Playwright 是可选依赖，默认不内置。缺失时脚本会返回 error code `3`、`install_hint` 和计划产物路径。用户已有 HAR、console、RUM/Sentry、trace id 或 request id 证据时，不要把缺少 Playwright 当成阻塞。

手动兼容采集的 `trace.zip` 可能包含 DOM、Cookie 或请求体，不得登记到持久化 Case；进入 Studio 闭环时仍以 HostVerifier 生成并二次扫描的脱敏 artifact 为准。

## HAR 流程

从 workspace root 运行：

```bash
python3 skills/frontend-repro-investigator/scripts/har_analyzer.py --file <path-to.har>
```

脚本返回：

- `failed_requests`：浏览器里的 4xx / 5xx 请求。
- `slow_requests`：耗时至少 1000 ms 的 API 请求。
- `frontend_findings`：静态资源/chunk 失败、source-map 缺失、缓存风险、CSP header、CORS/auth redirect、mixed-content/TLS block 和 GraphQL 错误。
- `backend_handoff.trace_ids`：从 `x-trace-id`、`x-request-id`、`traceparent` 等 header 提取的值。
- `backend_handoff.candidate_endpoints`：可映射到后端服务的 API path。

## Console / RUM 流程

如果用户提供 console 文本或 RUM/Sentry 链接，而不是 HAR：

从 workspace root 运行：

```bash
python3 skills/frontend-repro-investigator/scripts/console_analyzer.py --file <path-to-console-or-sentry.json>
```

如果证据是粘贴文本，先保存到本地临时文件，或通过 stdin 输入：

```bash
pbpaste | python3 skills/frontend-repro-investigator/scripts/console_analyzer.py
```

脚本返回：

- `frontend_findings`：console API 失败、JS stack 证据、ChunkLoadError、CSP violation、mixed-content/TLS block 和 source-map reference。
- `backend_handoff.trace_ids`：从 `x-request-id`、`request-id`、`x-trace-id`、`trace-id` 和 Sentry-like `trace_id` 字段提取的值。
- `backend_handoff.candidate_endpoints`：从 URL 中提取的 `/api/...` 和 `/graphql` path。
- `redacted_input_preview`：已脱敏预览，隐藏 token、password、secret、authorization、cookie、bearer token 和敏感 query 值。

然后按下面规则处理：

1. 如果是 `ChunkLoadError`、`Loading CSS chunk failed` 或静态资源 404，优先按前端发布/缓存/CDN 方向分类。
2. 如果分析结果包含 API URL、HTTP status、request id 或 trace id，用 `backend_handoff` 继续交给后端取证。
3. 如果只有 JS stack，用 `routing/references/frontend-entry-map.yaml` 识别前端仓库，再检查相关组件或 request wrapper。

## Sentry 事件查询

如果用户提供 Sentry event id，且工作区有 Sentry 权限，从 workspace root 拉取并归一化事件：

```bash
SENTRY_AUTH_TOKEN=<token> python3 skills/frontend-repro-investigator/scripts/sentry_fetch.py \
  --base-url <sentry-base-url> \
  --organization <org-slug> \
  --project <project-slug> \
  --event-id <event-id>
```

也可以用 `--token <token>` 替代 `SENTRY_AUTH_TOKEN`。脚本调用：

```text
GET /api/0/projects/{org}/{project}/events/{event_id}/
```

脚本返回：

- `summary`：event id、message、trace / endpoint / stack 数量。
- `frontend_findings`：Sentry message、endpoint 证据和 JS stack 摘要。
- `backend_handoff.trace_ids`：递归提取的 `trace_id`、`traceid`、`x-request-id`、`request-id`、`x-trace-id` 和 `trace-id`。
- `backend_handoff.candidate_endpoints`：仅保留 `/api`、`/api/...` 和 `/graphql` path。
- `redacted_event_preview`：隐藏敏感 query 值和 bearer token 后的事件预览。

排障模式下才用归一化的 `backend_handoff` 继续后端排查。验证模式下，把归一化证据返回给 `bug-verifier`。归一化输出已经足够时，不要粘贴原始 Sentry event JSON。

## 证据合并

有多个分析结果时，先合并再交接后端：

```bash
python3 skills/frontend-repro-investigator/scripts/evidence_merge.py \
  --frontend-entry-map skills/routing/references/frontend-entry-map.yaml \
  /tmp/har-analysis.json \
  /tmp/console-analysis.json \
  /tmp/sentry-analysis.json
```

合并结果会保留一个 `backend_handoff`，包含去重后的 trace ids、排序后的 candidate endpoints、从 `frontend-entry-map.yaml` 得到的 candidate services、endpoint 来源元数据和合并后的 frontend findings。优先使用合并后的 `trace_ids`；没有 trace id 时，先用 `candidate_services`，再结合 `routing/references/frontend-entry-map.yaml` 使用排序后的 `candidate_endpoints`。

## 后端交接

HAR / console / RUM 分析后：

1. 读取 `routing/references/frontend-entry-map.yaml`。
2. 将 `candidate_endpoints` 匹配到已知 API base URLs 和候选下游服务，并从失败请求提取准确的 method/path。
{{ if hasSkill . "service-topology-query" }}
3. method/path 提取完成后，先查询有界正式拓扑；只有一个已确定的 `candidate_service` 时传 `<入口服务>`。存在多个候选或尚不能确定服务时省略 `--service`，让查询器返回全部排序候选继续取证，不任选一个：

```bash
python3 skills/service-topology-query/scripts/query.py \
  --service <入口服务> --method <失败请求 method> --path <失败请求 path> \
  --max-depth 3 --json
```

拓扑路径只作后端导航证据。逐跳用 trace、同窗口日志和 runtime 验证；与 trace 冲突时以 trace 为准。查询返回 `no_match/unavailable` 时回退 `frontend-entry-map.yaml`、routing 和 endpoint 日志查找。
{{- else }}
3. 本生成物未包含 `service-topology-query`，继续用 `frontend-entry-map.yaml`、routing 和 endpoint 日志查找候选后端。
{{- end }}
4. 如果有 trace/request id，用同一时间窗口调用 `tracing-query` 和日志查询 skill。
5. 如果没有 trace id，用 endpoint path、user id、session id 和发生时间查日志。
6. 如果后端没有收到请求，排障模式下才保留 frontend / CDN / network / CORS / auth 方向候选。

验证模式下，本节只作为证据收集指南。返回 `backend_handoff` 和浏览器实际表现，不要声称后端、前端、CDN、鉴权或网络根因。

## 输出契约

始终返回这个结构：

```yaml
frontend_evidence:
  page: "<page-url-or-route>"
  action: "<user-action>"
  artifact_type: "har|console|rum|manual"
  browser: "<browser-version-if-known>"
findings:
  - type: "api_5xx|api_4xx|slow_api|static_asset_failed|chunk_load_error|csp_violation|mixed_content_or_tls_block|cors|js_exception|backend_not_reached"
    evidence: "<specific request/error/stack>"
backend_handoff:
  env: "<env>"
  endpoints: []
  trace_ids: []
  candidate_services: []
next_skills:
  - "tracing-query"
  - "elk-log-query"
```

## 硬约束

- 只读取本地证据和观测数据。
- 引用前必须脱敏 token、cookie、password、authorization header 和个人标识。
- 浏览器证据没有关联到后端 trace / log / runtime 证据前，不能声称后端根因。
- 验证 Agent 使用时，完全不能输出根因判断。
