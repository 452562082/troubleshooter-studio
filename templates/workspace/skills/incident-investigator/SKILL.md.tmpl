---
name: incident-investigator
description: 排障主线 skill。固化"症状→时间轴→横向→纵向→三向交叉→根因→沉淀"7 步流程,任何"X 报错/慢/不通/突增/失败"问题先用本 skill 编排其它 skill,不要直接跳具体 skill。
---

# Incident Investigator(7 步排障图谱)

```
Step 1  收齐前提 + 症状先验证 + 历史 incident 复查
Step 2  timeline → ±5 分钟内变更(deploy / 配置 / git)+ diff_risks 自动分类
Step 3  ns-snapshot → 孤立 vs 广播
Step 4  cascade_check → 沿依赖图追下游
Step 5  多向交叉 → trace + log + metric + 代码 + 数据(按问题类型选维度,详见下方)
Step 6  根因标注 + 决策树 + 故障快报
Step 7  沉淀(必做,confidence=high 必跑):pattern 追加到 known-errors.local.yaml,机器人越用越懂
```

## 证据维度全景(Step 5 选维度的依据)

排障证据有 **5 大维度**,LLM 天然偏向查容易的(日志、指标),**漏代码 + 漏数据是最常见的偏科**。Step 5 之前先按问题类型对标必查维度:

| 症状关键词 | 必查维度 | 可选维度 |
|---|---|---|
| **数据/状态类**:数据不一致 / 余额不对 / 订单状态异常 / 显示错乱 / 漏发货 / 重复扣款 / 漏发消息 | **数据库当前值** + **代码业务逻辑** + **网关 response_body** + 日志事务时序 | 链路 |
| **逻辑/计算类**:金额算错 / 优惠没生效 / 规则没触发 / 限流没生效 | **代码计算路径** + **数据库输入数据** + **网关 response_body**(批量接口) + 日志中间结果 | 指标 |
| **性能类**:慢 / 超时 / 卡 / P95 升高 / 响应延长 | 指标 + 链路慢 span + **数据库慢查询** + **代码热点路径** | 日志 |
| **可用性类**:5xx 突增 / down / 不通 / 报错暴涨 | 日志 error pattern + 指标错误率 + **代码近期变更**(`git log`) | 链路 + 数据库(若 query 异常) |
| **环境/网络类**:某 env 不通 / 跨服务断连 / DNS 失败 | 指标 + 日志 + ns-snapshot + cluster events | 代码(若超时配置变了)|
| **客户端/用户体验类**:页面白屏 / 按钮没反应 / 卡顿 / 上传失败 / 渲染错乱 / 某浏览器或机型独有 / console 报错 / 用户描述"页面慢"或"加载不出来" | **HAR 文件 / 浏览器 Network panel** + **RUM 错误堆栈**(Sentry / 神策 / ARMS / 自建 console 收集,**让用户主动提供链接或截图**) + **客户端实际 bundle 版本/缓存状态** | 链路 trace(看后端是否真收到请求,关联 `X-Trace-Id` header)+ 后端日志(关联 user_id / session_id)|

**判断逻辑**:命中 ≥1 个关键词 → 该维度从"可选"升"必查";没命中关键词 → 默认按可用性类。

**取证 skill 索引**:
- 代码:routing 拿到仓库路径后用 `grep` / `glob` / `read`(IDE 自带工具),找类/函数/最近 git diff
- 数据库:用对应 `<type>-runtime-query` skill(mongodb / mysql / postgresql / redis / es / clickhouse / kafka 等)
- 链路:`tracing-query`(jaeger 或 tempo 或 skywalking)
- 日志:`elk-log-query` 或 grafana-loki mcp
- 指标:grafana-prometheus mcp

## Step 1:收齐前提 + 验证 + 历史 grep

**1.1 前提**:`entity ID` + `时间窗` + `env` + `service`。任一缺失只反问**这一个**澄清。
若接口名匹配 `import/sync/batch/bulk + 复数 s`(如 `importPgcVideos` / `syncOrders`)
且用户描述含 "N 个共 M 条" / "导入 N 部,M 集" 这种 **N×M 数量词**,
额外只问一句:"是 **1 次请求 batch M 条**,还是 **N 次请求各 ~M/N 条**?"
**只在双重命中(接口名 + N×M 数量词)时触发**;性能类描述"batch 查询慢"不触发。

**1.1c 客户端类问题强制澄清**:症状含 "白屏 / 卡顿 / 按钮没反应 / 渲染错乱 / 某浏览器/机型独有 /
页面慢 / console 报错 / 上传失败" 等**客户端关键词**(后端类"接口慢"不触发)→
不要直接进 5.1 trace 查询(机器人 5 维表里"客户端/用户体验类"必查的 HAR/RUM/bundle 都得用户主动给),
额外问一组:
"用的什么**浏览器 + 版本**?**哪个页面 + 哪个交互**触发?能给一份 **HAR 文件 / RUM 错误链接(Sentry/神策/ARMS)/ console 截图**吗?
后端有对应的 **trace_id / request_id** 吗(HAR 里通常在 `X-Trace-Id` request header)?"
拿到 HAR/RUM/console 之一才进 Step 2,**禁止纯凭"用户说慢" 就开始查后端 trace**(后端视角看不到客户端真相)。

**1.2 症状先验证**(30 秒,防无效排障):
跑 `baseline_query_templates.error_rate_now` 看症状仍在?
- 仍异常 → 继续
- 已回 baseline → 问用户"已恢复,要复盘还是继续监控?"
- selector 全 0 命中 → 服务名/env 拼写问题,反问
- 拉不到指标 → 不阻塞,但记下来回头查

**1.3 历史 grep**(沿用 routing skill 的 known-errors 速查表,不重复实现):

把症状里的关键报错串 / 异常文本提取出来作为 `<error pattern>`,然后 grep 两份故障库:

```bash
# 两份都扫:known-errors.yaml(模板内置常见模式)+ known-errors.local.yaml(历次 sink 的本团队特有)。
# .local.yaml 可能不存在(从未 sink 过),grep 退码非 0 不阻塞。
grep -E "<error pattern>" \
  routing/references/known-errors.yaml \
  routing/references/known-errors.local.yaml 2>/dev/null
```

命中 → 提示"匹配历史 pattern X,典型根因 / next_actions 见命中条目";**置信度上限调高一档**(有历史支撑);带 `causation_chain.check_downstream_for` 的 pattern 主动跑 `cascade_check.py` 看下游(详见 routing skill "关键用法 1")。

## Step 2:时间轴对齐(最高 ROI 一步)
{{ if hasSkill . "recent-changes" }}
```bash
python3 skills/recent-changes/scripts/timeline.py --env <env> --service <svc> --since 1h \
    --incident-time "<故障开始时间>"
```
{{- else }}
> 本系统未启用 `recent-changes` skill —— 手动对齐:在主角 repo 跑 `git log --since=1h` + 查配置中心 history,找故障窗 ±5 分钟内的 deploy / 配置改。
{{- end }}
输出按时间倒序,`correlated:true` 标记 ±5 分钟内变更。**故障 ±5 分钟内有 deploy / 配置改 → 强相关,优先怀疑**。
看 `event.diff_risks` —— `timeout_decreased / rate_limit_decreased / replicas_decreased` 等高危类型直接定型变更危险等级。

## Step 3:横向扫(孤立 vs 广播)
{{ if hasSkill . "k8s-runtime-query" }}
```bash
python3 skills/k8s-runtime-query/scripts/k8s_query.py --env <env> --cluster <c> ns-snapshot --namespace <ns>
```
{{- else }}
> 本系统未启用 `k8s-runtime-query` skill —— 横向扫改用可观测性维度:看 cluster / namespace 级指标与日志是否多服务同时异常,判断孤立 vs 广播。
{{- end }}
- 只主角异常 → **孤立**,Step 4
- 多服务一起异常 → **广播**(基础设施 / 网络 / 共享 DB),看 cluster events / kube-system

## Step 4:沿依赖图追下游

```bash
python3 skills/incident-investigator/scripts/cascade_check.py --env <env> --service <主角> \
    --cluster <c> --namespace-default <ns>
```

按 `summary.verdict` 走:
- `isolated_downstream` + `verdict_root_likely=user` → 把 user 当主角递归 Step 4 再追一层
- `widespread_downstream` → 共享依赖问题,不要继续递归
- `all_healthy` → 真因在主角自身,Step 5
- `no_downstream_in_map` → dependency-map 没填,提示用户去填;**置信度上限 medium**

## Step 5:多向交叉(trace + log + metric + 代码 + 数据)

按"证据维度全景"选维度。最低 3 维起步,**数据/逻辑类问题代码 + 数据库不可省**。

### Step 5 子查询通用输出契约(5.1 / 5.2 / 5.3 / 5.4 每个都遵守)

每个子查询输出 `evidence_seen` 必填;**`missing_critical_evidence` 仅当 LLM 自评 "证据不足以做出可靠推断" 时列**
(不是每次都必出 —— 信息已充分时空着即可,避免过度防御让 confidence=high 拿不到 / Step 7 沉淀跑不起来):

```yaml
evidence_seen:                    # 实际看到的数据(必填)
  - "..."
missing_critical_evidence:        # 仅自评不够时列(可空,空着不写本字段)
  - what: "..."                   # 缺什么(具体到字段/行/记录,不写"更多日志"泛指)
    why_critical: "..."           # 为什么关键(缺它哪个候选 explains_all_obs 会翻 no)
    where_to_get: "..."           # 从哪取
    or_acceptable_proxy: "..."    # 次优替代(没替代写 `none`)
```

**列出来的必须是真 critical**:缺了这条,Step 6 差分诊断里至少 1 个候选的 `explains_all_obs` 会从 yes 翻 no。
nice-to-have / 已有充分代理证据 / 取证 ROI 极低 —— 这些不写,**空着比凑数好**。

**Step 6 入口拦截**(差分诊断段会再次引用):

任一子段 `missing_critical_evidence` 非空 → 进入 Step 6 前**强制走 ASK_USER**:逐条列给用户,问"能补吗"。
用户响应三分支:

- **(a) 帮取到** → 加入 `evidence_seen`,该 missing 出表;若还有其它 missing 继续 ask 直到清空
- **(b) 明确无法取得**(不在采集范围 / 已过 TTL 等)→ 标 `unavailable`,Step 6 用现有证据出结论但
  **confidence 上限锁中** + 故障快报明示 "以下证据 unavailable,结论可能有 X/Y 风险"
- **(c) 先看现有的**(用户主动跳)→ 标 `skipped`,Step 6 用现有证据出结论但
  **confidence 上限锁低** + 故障快报明示 "以下证据未取,结论可能漂移:..."

**跟差分诊断的闭环**:差分诊断里全部候选 `explains_all_obs: no` → 把 "无法解释的现象"
逐条补到对应子段的 `missing_critical_evidence` 然后走上面三分支,不允许直接锁低 confidence 收尾。
**两个机制对偶**:差分诊断帮 LLM 排除错误假设,本契约帮 LLM 主动找缺失证据。

### 5.1 trace + log + metric 三向

**调 grafana mcp 工具的 datasource 排他契约**(防止多 datasource 重复查):

用户在 wizard 已经为本系统选定了"排障专用 datasource UID",查 loki / prometheus / jaeger 时**必须显式传 `datasourceUid` 参数**,值从 routing 的 `references/observability-map.yaml` 取:
- loki:`loki.datasource_uid_by_env.<env>` 或 `loki.label_mapping_by_env.<env>.grafana_ds_uid`
- prometheus:`prometheus.datasource_uid_by_env.<env>`
- jaeger:`jaeger.datasource_uid_by_env.<env>`

**禁止先调 `list_datasources` 然后看到多个 loki/prometheus 就挨个试** — 用户已经选定唯一 UID,其他 datasource 就算 grafana 里存在也是噪音。**找不到对应 env 的 UID 直接报"该 env 没配 datasource_uid_by_env,请回 wizard 补",不要瞎选**。

并发拉三处后**喂 triangulate.py 做客观对齐**(不要 LLM 心算):

```bash
cat <<EOF | python3 skills/incident-investigator/scripts/triangulate.py --from-stdin
{"incident_window":"...","metric_anomalies":[...],"log_pattern_hits":[...],"trace_outliers":[...]}
EOF
```

不带 `--from-stdin` 跑,脚本输出 schema 模板。

`correlation` 字段直接给 `confidence: high/medium/low` + `consensus_service` —— **照搬,不要主观推**。

`critical_span` 选取规则:① 优先 `status=error` 的最深 span;② 没 error 找 duration 最长的非根 span;③ 跳过纯网络/sidecar/lb 代理 span。

### 5.2 代码取证(数据/逻辑/可用性类必查)

routing 给到仓库路径后:

```bash
# 1. 最近变更(可用性类:5xx 突增,先看变更)
cd <repo_root> && git log --oneline --since="2 hours ago" -- <主角 service 子模块>
git diff HEAD~5..HEAD -- <可疑路径>

# 2. 业务逻辑取证(数据/逻辑类:状态/计算/规则)
grep -rn "<关键报错串|关键业务字段名>" <repo_root>/<service>/ --include="*.go" --include="*.java" --include="*.py"
# 看到候选文件 → Read 完整函数,理解状态机/分支条件/异常处理
```

`证据`段必须给:**文件路径:行号** + 关键代码片段 + 推断("此处 if 条件在 status=X 时漏处理 Y → 解释了日志里的 Z")。
**只看日志报错串拼根因不算代码取证**,要落到具体代码行。

### 5.3 数据取证(数据/状态/逻辑类必查)

调对应 `<type>-runtime-query` skill(优先用 mcp tool,fallback CLI):

```text
# 数据/状态类:把"用户说的现象"对照"数据库实际值"
- 订单 #X 状态:db 里是 paid 还是 unpaid?(对照用户说"付了钱没发货")
- 余额:db 当前 + 流水表加总是否对得上?
- 缓存 vs DB 差异:Redis 里的值 vs MySQL 里的值,是否过期/不同步?

# 逻辑/计算类:把"代码的输入"和"代码的输出"都从 db 拿
- 优惠规则配置表(代码读这里) + 用户实际下单数据(代码处理这条) → 跑一遍逻辑应得 X,实际 Y
```

`证据`段必须给:**表名 + SQL/查询条件 + 返回行数 + 关键字段值对比**。

### 5.4 调用方/响应方实际 payload(数据/逻辑类必查)

**触发**:5 维表第 1/2 行命中(数据/状态类 或 逻辑/计算类)。
性能/可用性/环境类此节直接跳过,不绕路。

**取证内容**:

```bash
# 1. 网关访问日志 / 接入层入口日志 / API gateway access log,任一可达即可
#    取每次请求的 request_body 长度 + response_body 完整内容
#    数据源优先级:专用网关日志 > 业务侧 access log fallback
grep "<request_id|entity_id|trace_id>" <gateway_log> \
  | jq '{rid: .request_id, rbody_len: (.request_body|length), resp: .response_body}'
```

**partial-success 接口识别**(response_body 含 `results[]` / `items[]` / `errors[]` /
`failed[]` 或任何"数组 + 数组元素带 code/error 子字段"的形状):

- 按 `results[i].code` / `results[i].error` 分组统计:**成功条数 / 失败条数 / 失败原因 top 3**
- 调用方说的"总数" vs `response.results.length` vs db 实际行数 → **三层数量对比**
- 差异落在 "调用方≠response" → 客户端/网关裁剪;落在 "response≠db" → 业务校验/事务

**证据**段必须给:**网关日志查询条件 + response_body 关键片段(可截断,
但 results 分组统计必须完整) + 三层数量对比表**。

**网关日志拿不到的 fallback**:
- 退到入口业务日志找请求-响应配对(`elk-log-query` + `trace_id` 关联)
- 仍拿不到 → 本维度标 N/A,confidence 上限锁中,
  快报里提示用户补 `routing/references/observability-map.yaml` 的 `gateway_log` 字段

### 5.5 主动 curl 重放(客户端 + 资源加载 / API smoke test 类必试,先于让用户贴 HAR)

**触发**:5 维表第 6 行命中(客户端/用户体验类),且症状涉及**单个资源加载**或 **API smoke**:
看不到图片 / 视频无法播放 / 字体没渲染 / CSS 加载失败 / JS bundle 404 /
"调用方说 API 失败但前端展示 OK" 这类**机器人能直接验证一遍前后端立刻分流**的场景。

curl 比让用户贴 HAR 快得多,**先试 curl 再决定要不要要 HAR**。

**取证手法**(按需挑,不必全跑):

```bash
# 1. 看 status / Content-Type / Cache-Control / 大小(最常用)
curl -I <URL>

# 2. 下载验证内容跟 mime 是否一致(200 但内容损坏的常见陷阱)
curl -L -o /tmp/probe <URL> && file /tmp/probe && ls -la /tmp/probe

# 3. 防盗链测试(CDN 配置 Referer 校验)
curl -H "Referer: https://<正确域名>" -I <URL>
curl -H "Referer: https://<假冒域名>" -I <URL>

# 4. UA 模拟(跟反幻觉自检 #4 "仅某浏览器" 配合,复刻 Safari/iOS 13 等)
curl -A "Mozilla/5.0 ... Safari/14.0" -I <URL>

# 5. Range request(视频 / 大文件断点续传探测)
curl -H "Range: bytes=0-1024" -I <URL>

# 6. CORS 跨域校验
curl -H "Origin: https://<前端域名>" -I <URL>
```

**判定**(curl 返回 → 前端/后端定位):

| curl 看到 | 大概率结论 | 下一步 |
|---|---|---|
| 4xx / 5xx | **后端**(资源不存在 / 鉴权 / 限流) | 拿 `X-Trace-Id` → 5.1 后端 trace |
| 200 + `Content-Type` 跟实际不符(图片返了 HTML) | **后端处理 / CDN 回源失败** | CDN 状态 + 后端处理 |
| 200 + 内容损坏(`Content-Length` 异常 / `file` 探测格式不对) | **后端转码 / 上传断裂** | 后端处理流程 |
| 200 + 全正常 | **前端代码 / 浏览器策略**(`<img src` / `<video src` / CSP / lazy load) | 走 5.2 代码取证 |
| 加 Referer 通 / 不加 403 | **CDN 防盗链配置** | CDN 配置审计 |
| 默认 UA 通 / Safari UA 403 | **后端 UA 黑名单 / 兼容性**(自检 #4) | 后端策略 / 浏览器兼容 |

**curl 看不到的**(跳出本节回到让用户贴):

- **JS 执行 / 渲染逻辑**(React/Vue state bug)→ curl 只拿 HTML/JSON 跑不了浏览器 → 让用户贴 **console error 截图**
- **用户特定 session 状态**(cookie / localStorage 不兼容)→ curl 默认无状态 → 让用户用 F12 **抓 HAR**
- **性能 profile / 长任务阻塞** → curl 无渲染 → 提示 "需要浏览器 DevTools Performance,机器人目前看不了"

**4 平台兼容性**:OpenClaw / Claude Code / Cursor / Codex CLI 都通过原生 Bash/Shell 工具跑(同 Step 2-3 scripts 调用)。
Codex 用户需要先在 `~/.codex/config.toml` 开 `[sandbox_workspace_write] network_access=true`,
`tshoot install --target codex` 会探测打印修复指引。

## Step 6:根因标注 + 处置建议

### 候选假设差分诊断(出根因前必跑,所有问题类型通用)

LLM 排障最大盲区是 **"找证据支持单一假设"** —— 拿到 3-4 个证据就拼自洽故事,
不去想"还有什么别的解释能套上这些证据"。强制差分诊断 = 医学式
**列候选 → 找反证 → 排除剩下**,本节是 Step 6 一切其它判断(置信度/反偏科/处置建议)的**前置必跑**。

**跳过条件**(满足任一即跳过):

- Step 1.3 known-errors 命中,且 Step 5 维度证据跟 pattern 的 `typical_cause` 完全吻合不矛盾 → 信任速查表
- Step 2 时间轴撞 ±5min 内变更,且 `event.diff_risks` 命中高危类型
  (`timeout_decreased` / `replicas_decreased` / `rate_limit_decreased` 等)→ 变更撞窗本身就是强证据,
  不必走差分(简单 case 走过度防御反而拉长流程)

**约束**(软化:有合理对偶必列;无对偶不强求 ≥2,但仍走 `explains_all_obs` 闭环):

- **有合理对偶候选必列**(防确认偏差):
  - 推 "下游 timeout" → 对偶候选必含 "本服务自身慢"
  - 推 "代码 bug" → 对偶候选必含 "数据/配置异常导致代码进错分支"
  - 推 "服务是 stub" → 对偶候选必含 "业务校验/鉴权早返回"
- **找不到合理对偶时,单候选 + 完整反证即可**(禁止凑稻草人充 ≥2,空着 比凑数 更准)
- 每个候选给 4 个字段:
  - `supports`: 哪些观察支持它
  - `refutes`: 哪些观察否定它(空写 `none`)
  - `explains_all_obs`: 能解释**所有**已观察现象 `yes/no`
  - `verdict`: `confirmed` / `rejected` / `undecided`

**结论规则**(直接绑死 confidence,跟 Step 5 通用输出契约的 `missing_critical_evidence` 闭环):

- 所有候选 `explains_all_obs: no` → 把 "无法解释的现象" 逐条**补到 Step 5 对应子段的 `missing_critical_evidence`**
  然后走 Step 5 通用契约的 ASK_USER 三分支(a/b/c),**不允许直接锁低 confidence 收尾** ——
  让用户决定补证还是接受降级;无论走哪条分支,**禁止给改 prod 命令**
- 仅 1 个 `confirmed` 且 `explains_all_obs: yes` → 该候选作为根因,confidence 按下方量化规则评
- 多个 `confirmed` 二选一无法决断 → **confidence 锁中** + 列出 "区分两者所需的最小补证"
  (同样以 `missing_critical_evidence` 格式给出,方便用户对照取)

**例**(importPgcVideos case):

```yaml
observations:
  - duration=0.286ms
  - 0 mongo span
  - trace.app.biz.code=0
  - 网关 response.results[0].error="parent.title 超过 200 字"

hypotheses:
  - name: "服务是 stub / 空实现"
    supports: [duration<1ms, 0 mongo span]
    refutes:
      - prod env + 镜像 tag v2.3.1 非 stub-mock(查 deployment manifest)
      - response 有具体 results[] 业务错误,不是 stub 的 mock 返回形状
    explains_all_obs: no   # 不能解释 results 数组里的业务错误
    verdict: rejected

  - name: "业务参数校验早返回"   # ← 与初始 "stub" 直觉相反的对偶候选
    supports:
      - duration 极短(参数校验前就 return,没进业务逻辑)
      - 0 mongo span(没写 db)
      - results[0].error 明确指出校验失败原因
    refutes: none
    explains_all_obs: yes
    verdict: confirmed

  - name: "鉴权/限流挡住"
    supports: [duration<1ms]
    refutes:
      - HTTP 200(鉴权挡是 401/403,限流挡是 429)
      - response 有完整业务结构,不是鉴权/限流的简单 body
    explains_all_obs: no
    verdict: rejected

conclusion: 业务参数校验早返回(parent.title 超 200 字)
            → 走 next_actions 拉每条 request 的 title 长度对账,
              查规则是否最近改过(200 字限制是不是新加)
```

**为什么放在置信度量化之前**:置信度的"维度数"打分只回答"看了多少",
差分诊断回答"看到的能不能排除其它解释" —— 后者是准确性的真护栏,前者是覆盖率的代理指标。
没经过差分就给 confidence=high 是把"自洽故事"当"已验证结论"。

### 置信度量化(必须按规则判)

| 等级 | 条件 |
|--|--|
| 高 | **≥3 个证据维度交叉一致** + Step 2 时间轴撞上变更 + Step 4 依赖图能解释传导(**全满足**)。数据/逻辑类问题中的"代码"和"数据库"维度计入必算项,**没查代码或没查 db 直接降到中** |
| 中 | 维度数 ≥2 但 <3 / 时间轴或依赖图任一缺 |
| 低 | 单证据 / 仅推断 / 无法重现 / **数据问题但只有日志没有 db 证据** |

快报里**明示判定理由 + 维度自检**(`置信度: 中(日志 ✓ 指标 ✓ 代码 ✗ 数据库 ✗ 链路 N/A;数据类问题缺 db 证据降级)`)。

**反偏科兜底**:数据/逻辑/状态类问题(关键词命中"数据/状态/计算/规则"那两类)若只查了日志+指标 → confidence 上限锁 `低`,快报里必须列"未查的维度"让用户决定要不要补。

**结论自检**(出根因前必跑,**只对命中的推断生效**,没命中静默不干预):

1. 推断里含 "**服务是 stub / 空实现 / 镜像跑的不是预期版本**"
   → 必须有 镜像源码 OR k8s deployment manifest OR 启动日志 **三选一实锤**;
   缺实锤 → confidence 锁 `低`,结论改写为
   "**疑似** stub,待补 X/Y/Z 验证"。
   反模式:仅凭 trace duration 极短 + 0 db span 推 stub —— 这俩特征更可能是
   **参数校验 early return / 鉴权限流 / 缓存命中 / 业务前置校验失败**,
   线上 prod 跑 stub 概率极低,不要默认走 stub 解释。

2. 推断引用了 **trace tag 上的 code 字段**(`app.biz.code` / `rpc.grpc.status_code` /
   `dubbo.status` / `thrift.status` 等)→ 这些是**框架/传输层**码,
   `=0` 只代表 RPC 链路本身没崩,**不等于业务成功**;
   必须从 `response_body` 业务字段(`Response.Code` / `result.code` / `data.errno` 等)
   二次确认,否则该结论降级一档。

**客户端类问题反幻觉自检**(只在 5 维表第 6 行命中时生效,后端类问题这段静默):

3. HAR 显示 `status: 200` 但 `response.body.error` 非空 → 跟 partial-success 接口同款陷阱,
   **不要因为 HTTP 200 就推"请求成功只是前端 render 出错"**;先按 `body.error` 看业务码失败原因。

4. RUM 错误堆栈是 **minified**(变量名 a/b/c,行号在 bundle.js 几万行)→ 直接读 stack
   推不出真实代码位置,**必须先 sourcemap 反推到仓库 src/ 行号** 再下结论;sourcemap 拿不到 →
   confidence 锁 `低`,改写为 "疑似 X 模块,需要 sourcemap 或前端协助定位"。

5. 用户加载的 **bundle hash / build version ≠ 仓库当前 build**(用户没强刷,CDN/浏览器缓存还在旧版)
   → **禁止推 "代码 bug"**(代码已经修过了用户没拿到),改写为 "用户本地缓存旧版本,引导清缓存 / 硬刷";
   核对方式:HAR 里加载的 `bundle-<hash>.js` 文件名 vs 仓库当前 build 输出的 hash。

6. 错误**只在某机型 / 某浏览器**(如仅 Safari 14 / 仅 iOS 13 / 仅 Edge legacy)→
   必须先核 UA + 兼容性表(`browser-compat-map.yaml` 若团队有维护)再下结论,
   **禁止默认推"代码逻辑错"**;更可能是该机型 API 不支持(fetch keepalive / IntersectionObserver / CSS gap 等)。

7. 用户描述 "**刚才能用现在不行**" / "**别人都能用就我不行**" →
   必查 (a) CDN TTL + 边缘节点回源失败 (b) 用户本地缓存版本 (c) 该用户独有的 session 状态
   (旧 cookie / localStorage 跟新代码不兼容),禁止直接推"代码 bug"或"后端崩"。

**runtime 配置源优先级自检**(通用,涉及任何 runtime 配置 / 连接 / 服务地址类推断时生效):

8. 推断里**引用了具体的 host / IP / 端口 / 数据库连接串 / 三方 API 地址 / 服务地址 / 超时值 / 限流阈值** →
   必须先确认**数据源是配置中心**(nacos / apollo / consul / kubernetes ConfigMap)的 **runtime 值**,
   **不允许直接信任代码仓库里的 `config.yaml` / `application.yml` / `.env` / `config.toml`** ——
   那些是**默认值 / 早期开发值 / 历史残留**,生产 runtime 大概率被配置中心覆盖。

   **反模式**:在代码仓库 grep 到 `mongo.uri = X.X.X.X:27017` 就当 "真实 mongo 地址" 推断 →
   完全可能 X.X.X.X 是 3 年前迁移前的地址,nacos 里早改成 Y.Y.Y.Y。

   **正确流程**(数据/状态类问题尤其严格):
   - 先调 `config-executor` 拿 nacos / apollo / consul 里**当前服务**的实际配置
   - 跟代码仓库里 config.yaml 默认值**双向对比**,**以配置中心为准**
   - 不一致时,代码 config.yaml 的值**禁止当根因证据**,只能作为 "历史/默认值参考"
   - 没接配置中心(`PrimaryConfigCenter.Type=env-vars` / `kubernetes` / 无)的场景才允许用代码默认值

   **特别警惕**:广告/订单/用户/支付类业务数据被删/异常,推断"外部 IP 入侵" 时必先核 nacos 真实
   mongo / mysql / redis URI ——很多 "入侵 IP" 是机器人误把代码里的过时默认地址当真造成的。

   **配置中心不可达的 fallback 规则**(关键 — 防 silent fallback 撞坑):
   - 调 `config-executor` 失败(nacos 脚本 login 不通 / apollo/consul mcp not found /
     网络不通 / namespace 拿不到 / 401 / 403)→ **绝对不允许 silent fallback 到代码 `config.yaml` 当 runtime 真值**
   - 失败这件事本身要**当作 `missing_critical_evidence` 列出**,走 Step 5 通用契约的 ASK_USER 三分支:
     - **(a) 用户帮拿** nacos 当前 service-config 里的 runtime 值 → 加入 evidence_seen 继续
     - **(b) 用户说 nacos unavailable**(开发期 / 用户没权限)→ confidence 上限锁中 + 故障快报明示
       "未能确认 runtime 真值,结论基于代码 config.yaml 默认值,X.X.X.X 可能不是生产真实地址"
     - **(c) 用户跳过** → confidence 上限锁低 + 故障快报明示风险
   - **文件头注释含 "本地" / "开发" / "fallback" / "default" / "示例" / "example" 关键词**的 yaml →
     降级为"参考值",**绝不允许当唯一证据**(本次 truss case 的代码 config.yaml 头注释就明示"本地开发环境
     中间件配置",agent 没看注释直接当 dev 真值)

### 回滚 vs 热修 决策树

```
故障在某次 deploy/配置 5 分钟内?
├─ 是
│  └─ 旧 revision 还活? → 强烈回滚 (kubectl rollout undo / nacos 改回)
│      不活 → 持续 < 10 分钟回滚接受短暂全断;> 10 分钟热修
└─ 否
   └─ < 5 分钟能写 patch? → 热修
      不能 → 有降级路径用降级,没有就扩容/重启续命
```

特殊情况:
- 业务峰值期 → 偏向降级 + 限流,不要回滚
- **数据已写脏** → **禁止盲目回滚**,先限流入口阻止继续写错
- 无法重现 → 不要乱建 P0,先加监控 / 重现路径

### P0 命令安全网(改 prod 类必须三段)

`confidence=high` 可输出 P0 命令;`medium` 必须前置标"先验证再执行";**`low` 禁止给修改 prod 的命令**,改成"先抓 X/Y/Z 证据再升 confidence"。

```bash
# === 1. PRE: 看现状(纯查) ===
kubectl rollout history deployment/commerce -n base-prod
# === 2. EXEC ===
kubectl rollout undo deployment/commerce -n base-prod --to-revision=7
# === 3. POST: 等 1-2 分钟验证 ===
kubectl rollout status deployment/commerce -n base-prod
# 拉 PromQL 看 5xx 是否回落
```

不要连成一长串。

## Step 7:沉淀到 known-errors.local.yaml(**强制做,confidence=high 必跑**)

要让本次故障变成机器人的肌肉记忆,得抽象成 pattern 追加到
`routing/references/known-errors.local.yaml` —— routing skill Step 1.3 grep 必看的速查表
(按 `pattern` regex 匹配错误日志/异常文本),命中即跳过 Step 2-6 主流程直走 next_actions(Step 1 收前提仍需做)。

写到 **`.local.yaml`** 而**不是** `known-errors.yaml`:后者是模板内置 / `tshoot apply/upgrade` 重灌时
被覆盖,沉淀清零;`.local.yaml` 是用户私有(`apply_helpers` 后缀识别免删),沉淀永久保留。

### 7.1 强制约束 — 故障快报必须以沉淀草稿结尾

`confidence=high` 时,故障快报最后一段**必须**是这块,不许跳:

````markdown
### 沉淀(已 sink 到 known-errors.local.yaml)

```json
{
  "pattern": "downstream\\.user\\.timeout.*[1-3]s",
  "typical_cause": "user 服务调用超时配置改太短(≤3s),下游 p99 高于此值即全 timeout",
  "next_actions": [
    "查 nacos history 看 downstream.user.timeout 是否最近改过",
    "对比下游 user 服务 p99,判断新 timeout 是否够"
  ],
  "mitigation": "回滚 nacos 配置 / 临时把 timeout 改回 10s+",
  "causation_chain": {
    "check_upstream_for": ["context deadline exceeded", "5xx"],
    "explanation": "本服务 timeout → 上游也会 timeout 报 5xx"
  }
}
```
````

紧跟这块**调脚本写盘**(实际执行,不是描述):

```bash
cat > /tmp/postmortem.json <<'EOF'
{"pattern":"...","typical_cause":"...","next_actions":[...],"mitigation":"..."}
EOF
python3 skills/incident-investigator/scripts/sink_postmortem.py --input /tmp/postmortem.json --env <env> --service <svc>
```

`confidence=medium / low` 时:跳过 7.1 整段,故障快报到 Step 6 就结束。

### 7.2 抽象原则(避免污染)

- `pattern` 写**关键词级 regex**,不写具体值(用 `[\d\.]+` 而非 `30`)
- `typical_cause` 写**机制级**描述(为什么发生),不写本次具体业务上下文
- `next_actions` 是**别人遇到能照做的**通用步骤,不是本次的特殊操作
- 命中 `diff_risks` 时把 risk hint 抄进 `typical_cause` 一定不出错

### 7.3 sink_postmortem.py 行为

- 读 JSON → 校验必填(`pattern` + `typical_cause` + `next_actions`)+ pattern 是合法 regex
- **去重**:已存在同 `pattern` 的条目跳过(stderr 打 `[skip] ... 已存在`)
- 首次 sink 自动创建 `known-errors.local.yaml` 带头部说明
- 追加注释 `# auto-sunk YYYY-MM-DD by incident <env>/<service>` 保留追溯

### 7.4 跳过的场景

- `confidence=low` / `medium` → 沉淀风险高,跳过
- 已存在相似 pattern → 脚本自动去重(用户/LLM 不用判断)
- 用户在故障快报里说"先不复盘" → 跳过

**沉淀 1 条好的 pattern,胜过 10 条噪音条目**。宁少不滥。
