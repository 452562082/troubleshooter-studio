---
name: incident-investigator
description: 排障主线 skill。把"症状 → 时间轴 → 横向广度 → 纵向深度 → 三向交叉 → 根因标注"固化成 6 步流程图谱,强制 agent 按图谱走完所有关联,不漏关键证据。任何用户问"X 报错/慢/不通/突增/失败"类问题时都先用本 skill 编排其它 skill。
---

# Incident Investigator(排障主线 skill)

## 何时使用

**默认入口**:用户用了任何排障关键词 —— "5xx 突增 / 报错 / 超时 / 慢 / 不通 / 排查 / 故障 / 定位 / 为什么 / 失败 / 卡住"——**不要**直接跳进 routing / k8s-runtime-query / config-executor 等具体 skill,**先按本 skill 的 6 步流程编排**,所有具体 skill 当作工具用。

## 反模式(看到下面这些回复立刻警觉,自己别犯)

- 拿到"prod commerce 5xx" 就开始查 commerce 的 pod / log / metric,**没看时间轴上的变更、没扫横向兄弟服务、没沿依赖图追下游** → 经常停在表象、漏真因
- 看完一个 skill 的输出就下结论 —— 单一证据不能下根因
- 把所有 skill 全跑一遍但不交叉关联 —— 等同没查
- 时间窗模糊就开始查 —— 没有 ±5 分钟窗口对齐就没"突变前后撞上变更"的能判断

## 6 步流程图谱(强约束,逐步走完)

```
┌──────────────────────────────────────────────────────────────────────┐
│ Step 1:收齐前提(entity ID + 时间窗 + env + service + 接口)         │
│ Step 2:时间轴对齐 → recent-changes 一次拉"故障时间 ±5 分钟内变更" │
│ Step 3:横向扫 → 同 ns 其它服务是否也异常?(孤立 vs 广播判断)        │
│ Step 4:纵向沿依赖图追 → 下游所有服务一口气并发查                     │
│ Step 5:三向交叉 → trace + log + metric 在异常时刻交叉验证           │
│ Step 6:根因标注 → 直接根因 / 深层根因 / 置信度,然后 P0/P1/P2 建议  │
└──────────────────────────────────────────────────────────────────────┘
```

### Step 1:收齐前提

按 SOUL.md 顶部"最高原则"+ AGENTS.md "首问反射":

- entity ID(订单号 / 用户 ID / trace_id / request_id 任一即可)
- 时间窗(精确到分钟最好,至少给"什么时候开始 / 什么时候结束")
- env(dev/prod 哪一个)
- service / 接口名

**任一缺失只反问这一个澄清问题,一次问完不要分多轮**。

### Step 2:时间轴对齐(必跑,这是最高 ROI 的一步)

跑 `recent-changes` skill 的 `timeline.py`,**并发拉**三处:

```bash
python3 scripts/timeline.py --env <env> --service <svc> --since 1h
```

输出格式按时间倒序合并:
```
14:25 [k8s] commerce-service rollout to revision 8 (image: commerce:v1.23)
14:23 [故障开始] 用户反馈 5xx 突增
14:18 [nacos] commerce/dev/application.yaml 改了 downstream.user.timeout 30s→3s
14:10 [git] commerce-service main: feat: 加 user 调用超时控制 (auth/abc123)
```

**判断**:
- 故障时间 ±5 分钟内有变更 → **强相关**,优先怀疑该变更
- 故障时间是变更后的几秒到几分钟内突变 → **大概率因果**,先回滚验证
- 时间窗内无变更 → 进 Step 3 看是否广播

### Step 3:横向扫(孤立 vs 广播)

跑 `k8s-runtime-query` 的 `ns-snapshot`:

```bash
python3 scripts/k8s_query.py --env <env> --cluster <c> ns-snapshot --namespace <ns>
```

输出每个 pod 的状态分布(健康/重启/Pending/CrashLoop)+ 异常 pod 列表 + 命中的 known-errors 类型计数。

**判断**:
- 只 commerce 的 pod 异常,其它服务健康 → **孤立故障**(应用问题),回 Step 4 沿依赖追根
- 多个不相关服务一起异常 → **广播故障**(基础设施 / 集群 / 网络 / 共享 DB),立刻看 cluster events / kube-system 节点

### Step 4:纵向沿依赖图追

跑 `cascade_check.py` —— 一条命令并发查所有下游的 K8s 状态层 + 列出要查的数据层 hints:

```bash
python3 scripts/cascade_check.py --env <env> --service <主角服务> \
    --cluster <c> --namespace-default <ns>
```

输出 JSON 字段:
- `results[]`:每个 downstream 服务的 verdict(healthy / degraded)+ phase 分布 + 异常 pod 列表 + 命中的 known-errors 模式
- `data_store_hints[]`:列每个数据层依赖 + 对应该调的 skill,**agent 必须主动跑这些 skill**(脚本不直接查数据层因为各类型 schema 太不一样)
- `summary.verdict_root_likely`:**第一个 degraded 的下游服务名,大概率是真因**
- `summary.verdict`:`all_healthy / isolated_downstream / widespread_downstream / no_downstream_in_map`

**判断**:
- `verdict=isolated_downstream` + `verdict_root_likely=user` → 把 `user` 当主角**递归回 Step 4** 再追一层
- `verdict=widespread_downstream`(多个下游同时异常)→ 共享依赖问题(数据库 / 网络 / 配置中心),不要继续递归
- `verdict=all_healthy` → 真因在当前服务自身,进 Step 5 看代码 / 配置层面
- `verdict=no_downstream_in_map` → **dependency-map 没填**,告诉用户去填 `service-dependency-map.yaml`,本轮排障跳过 Step 4(可能漏真因,**置信度上限 medium**)

#### 反模式

- 跳过 cascade_check 直接 LLM 推"应该是 X 服务问题" → 没数据支撑的猜测
- depth > 1 不慎用 → 下游的下游 token 暴增,排障变成全集群扫描

### Step 5:三向交叉(trace + log + metric)

异常时刻**同时**拉三个数据源,然后**用 `triangulate.py` 做客观对齐**(不要让 LLM 心算时间差和相关性):

#### 5.1 三路独立拉数据(并发)

- **trace**:走 `tracing-query` / `tempo-query` / `skywalking-query`(看 `observability-map.yaml` 的 `default_tracing` 决定),按 trace_id 或 service+时间范围拉,找耗时 TOP / 错误 span
- **log**:走 Loki / ELK,LogQL/ES 用 `selector_chain` 第一条命中,**grep `known-errors.yaml` 的 pattern 标命中类型**
- **metric**:走 Prometheus,QPS / p99 / 错误率 同窗口对比,**对比 24h 前同时段 baseline**(`<expr> offset 24h`),给出 baseline / actual / delta

#### 5.2 喂给 triangulate.py 做客观对齐

把三路结果拼成统一 schema 的 JSON,通过 stdin 喂给:

```bash
cat <<EOF | python3 scripts/triangulate.py --from-stdin
{
  "incident_window": "2025-04-29 14:20 ~ 14:30",
  "metric_anomalies": [
    {"metric":"p99","target_service":"inventory","baseline":"500ms","actual":"5s","delta":"+10x","start_ts":"2025-04-29T14:23:00Z"}
  ],
  "log_pattern_hits": [
    {"pattern":"context deadline exceeded","target_service":"inventory","count":847,"first_seen":"2025-04-29T14:23:12Z","last_seen":"2025-04-29T14:29:55Z"}
  ],
  "trace_outliers": [
    {"trace_id":"abc123","target_service":"commerce","duration_ms":8200,"critical_span":{"service":"inventory","operation":"getStock","duration_ms":7800},"start_ts":"2025-04-29T14:23:15Z"}
  ]
}
EOF
```

不带 `--from-stdin` 跑,脚本会输出 schema 模板让你照着拼。

#### 5.3 解读 correlation 字段(直接照搬,不再主观推理)

triangulate.py 输出的 `correlation` 节给客观结论:

```json
"correlation": {
  "metric_log_aligned": true,        // metric 突变窗口和 log error 窗口 ±60s 内重合
  "log_trace_aligned": true,         // log error 窗口和 trace 异常窗口 ±60s 内重合
  "trace_metric_aligned": true,      // 同上
  "aligned_count": 3,                // 0-3,几向对齐
  "consensus_service": "inventory",  // 三向同时指向哪个服务(关键!)
  "consensus_window": "...",
  "confidence": "high",              // high=三向 + 共识 / medium=两向 / low=单证据
  "verdict": "三向高度一致,共识指向服务 inventory,..."
}
```

**confidence 字段直接对应 Step 6 置信度量化标尺**——不要在快报里写"我觉得 90% 是 inventory",写 `triangulate.py` 给的 `high/medium/low`。

#### 反模式

- 三路独立看完,"我觉得他们都指向 X" → 这是主观推理,**必须经过 triangulate.py 做时间窗对齐 + service 集合交集运算后再下"三向一致"结论**
- 时间格式不统一就喂(混 UTC+8 / UTC) → triangulate.py 解析失败会进 notes,自查格式

### Step 6:根因标注 + 建议(故障快报模板)

按 AGENTS.md 的故障快报模板输出。**根因质量分级**:

- **直接根因**:有明确证据(具体 pod / 具体 SQL / 具体配置项 / 具体 commit),可重现
- **深层根因**:推断(为什么这个 pod 会 OOM:加载逻辑 / 流量峰值 / GC 配置)

#### 置信度量化标尺(必须按规则判,不要主观吹"高")

| 等级 | 严格判定条件(全满足才算) | 用例 |
|--|--|--|
| **高** | ① Step 5 三向交叉一致(metric 突变 / log error 模式 / trace 慢 span 都指向同一服务+同一时刻)<br>② Step 2 时间轴 ±5 分钟内有 correlated 变更<br>③ Step 4 依赖图能解释症状传导路径 | "14:18 nacos 改超时阈值 → inventory 调 user 全超时 → commerce 5xx",三向都对 |
| **中** | 任一缺失但其余两条满足 | 三向交叉只有 metric+log 一致,trace 没采到对应链路;或者三向一致但没找到时间轴变更(可能是流量峰值) |
| **低** | 单证据 / 仅 LLM 推断 / 无法重现 | 只看到 log 报错,metric 和 trace 正常;或者根因纯属"应该是 XX" |

**写在快报里要明示判定理由**,例:`置信度: 中(metric+log 一致指向 inventory,但 trace 数据缺失,无法验证调用链路)`,这样用户知道哪一环弱。

#### 处置建议:回滚 vs 热修 决策树

不要默认建"P0 回滚"或"P0 热修",按下面决策:

```
Q1: 故障开始时间是否在某次 deploy / 配置改动后 5 分钟内?(看 Step 2 timeline 的 correlated 标记)
├── 是
│   └── Q2: 旧 revision 还活吗?(rollout-history 看上一个 RS 的 replicas_available)
│       ├── 活 → P0: **强烈回滚**(`kubectl rollout undo` / Kuboard 回滚 / nacos 改回旧值)
│       └── 不活(已被新 RS 完全替代)→ Q3: 故障已持续多久?
│           ├── < 10 分钟 → P0: **回滚 + 接受短暂全断**
│           └── > 10 分钟 → P0: **热修上线**(回滚再起新 pod 时间窗 vs 热修打 patch 时间窗,后者快)
└── 否(跟 deploy/配置无关)
    └── Q4: 是否能在 < 5 分钟内写出 patch?
        ├── 能(单点改 1-2 行)→ P0: **热修**
        └── 不能(涉及业务逻辑) → Q5: 是否有降级路径?
            ├── 有(限流 / 降级开关 / 切走流量)→ P0: **降级**先止血,P1 慢慢修
            └── 没有 → P0: **扩容/重启**先续命,P1 紧急代码修复
```

特殊情况:
- **业务峰值期**(prod 上下班高峰 / 大促):回滚成本可能 > 热修;偏向降级 + 限流先扛过峰值
- **数据已经写脏**(订单 / 支付状态错):**禁止盲目回滚**,回滚后旧版本读新数据可能再错;先 P0 阻止继续写错(限流入口),再分析数据修复策略
- **无法重现**:不要乱建 P0 改动,先加监控 + 重现路径,免得反向引入新问题

#### 输出格式

故障快报模板第 5 节"处置建议"按这个填:

- **P0 止血**:从决策树挑出来的具体动作 + 命令 + 预期效果(10 分钟内可执行)
- **P1 修复**:`<代码改哪里 / 配置改什么>` + 测试方法
- **P2 预防**:监控 / 告警 / 健康检查 / 灰度策略 / SLO 调整

## 执行示例(给作者验证用,机器人按这个走)

**Q**:"prod commerce 接口 14:23 开始 5xx 突增,trace_id=abc123 用户付不了款"

按 6 步:
1. ✓ 信息齐(prod / commerce / 14:23 / trace_id 都有)
2. **timeline.py prod commerce --since 1h** → 发现 14:20 commerce 部署 v1.23 + 14:18 nacos 改了 downstream.user.timeout 30s→3s。强相关。
3. **ns-snapshot prod base-prod ns** → 只 commerce-3 个 pod 重启过,其它服务健康 → **孤立**,继续 Step 4。
4. 读 dependency-map → commerce 下游 user / order / inventory + mysql:order_db。**并发查**:
   - user pod / metric / log 全正常
   - order pod / metric / log 全正常
   - inventory pod 50% 重启过,关联 known-errors:"context deadline exceeded"
5. 三向交叉 inventory:
   - trace abc123 → inventory call 超时 8s
   - log inventory 报 "rpc to user timeout 3s"
   - metric inventory p99 10s
   → 三向一致,**确认 inventory 因调 user 超时阈值改成 3s 后,user 的 p99 是 5s,所以全部超时**
6. 根因:14:18 把 downstream.user.timeout 从 30s 改成 3s,但 user p99 实际 5s → inventory 调 user 全超时 → 上游 commerce 全 5xx
   - 直接根因:配置项 `downstream.user.timeout=3s` 过严
   - 深层根因:改超时阈值前没看 user 真实 p99 baseline
   - 置信度:**高**(时间轴 + 横向 + 纵向 + 三向交叉全对得上)
   - P0 止血:nacos 把 timeout 改回 30s 或 5s 看 user p99
   - P1 修复:代码侧加超时退避;监控加 user p99 告警
   - P2 预防:配置改动走灰度,改完 5 分钟看 dashboard

**这个流程跑完用户拿到的不是"commerce 报错了你看下"**,是直接可处置的 P0 命令。

## 跨 skill 协作

本 skill 是**编排者**,不直接调 HTTP API。所有具体能力走子 skill:
- `routing` 拿映射(env/service → ns/cluster/mcp/dataId)
- `recent-changes` 拉时间轴
- `k8s-runtime-query` 拉 K8s 状态
- `config-executor` 查配置
- `tracing-query` / `tempo-query` / `skywalking-query` 查 trace
- 数据层 skill 查 redis / mongo / es / mysql 等

## 硬约束

- **6 步不可跳过**(简单查询走简单查询路径,但只要用户用了排障关键词就走完整流程)
- **Step 2 时间轴必跑** —— 这是最高 ROI 的一步,没看变更就下结论几乎一定漏真因
- **Step 4 必读 dependency-map** —— 没读就下"是 X 服务自己的问题"是不负责任的
- **Step 6 置信度必须标** —— 给用户判断要不要相信你的根因
