---
name: kafka-runtime-query
description: 通过环境动态查询 Kafka 集群状态。先通过 routing 获取环境映射,优先用预注册的 kafka-<env> mcp tool 直查(tuannvm/kafka-mcp-server,franz-go 纯 Go 无 native deps;装机器人前用户需手动 `brew install tuannvm/mcp/kafka-mcp-server` 或从 GitHub Release 下 binary,不是 npx/uvx 零安装);mcp 不可用时 fallback kafka CLI(kafka-topics.sh / kafka-consumer-groups.sh)。
---

# Kafka 运行时查询

## 执行流程（固定）

1. 先确认目标环境与服务；环境映射统一通过 `routing` 获取。
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul") (eq .Infrastructure.PrimaryConfigCenter.Type "kuboard")}}
2. 通过配置中心获取该环境配置文本。
{{- else if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
2. 通过 `resolve_runtime_static.py --agent-id {{.AgentID}} --env <env>` 读取静态连接信息。
{{- end}}
3. 解析配置中的 Kafka 连接信息(broker 地址列表)。
4. **工具选用顺序**:
   - **优先 mcp tool**:本系统按 brokers dedup 在每个 env 注册 kafka MCP(`kafka-mcp-server` binary,tuannvm/kafka-mcp-server,MIT,franz-go 纯 Go)。**列工具看注册了几个 source**:
     - 只 1 个 `kafka-<env>`(单 cluster,典型)→ 直接 `tool_use` 调那个。
     - 多个 `kafka-<source>-<env>`(多 cluster)→ **必须查 routing 的 `references/service-to-datastore-source.yaml`** 拿 `service_routing.data_stores.kafka.<service>` 对应 source,只调 `kafka-<对应 source>-<env>`,**禁止瞎选**。
   - **mcp 不可用**(binary 没装 / 没在 PATH) → fallback **kafka CLI**(`kafka-topics.sh` / `kafka-consumer-groups.sh`,Kafka 标准发行版自带)。
   - **CLI 也没装** → fallback Python `kafka-python` 包脚本。
5. 返回证据:topic 列表、partition 分配、consumer-group 消费位点、积压量。

## MCP 安全契约(只读使用)

tuannvm/kafka-mcp-server 提供 9 个工具,其中 8 个读、1 个写:

读操作(排障主用):
- `cluster_overview` — 整集群 broker / topic / consumer-group 一览(快速诊断起点)
- `list_topics`
- `describe_topic` — 看 topic 元信息(partition / replication / config 摘要)
- `list_consumer_groups`
- `describe_consumer_group` — 看 consumer-group 消费位点、lag、成员
- `consume_messages` — 拉指定 topic/partition 的消息(诊断时常用)
- `list_brokers`
- `describe_configs` — 看 topic/broker 配置(替代了老版本的 get_topic_config / get_broker_config 两个分开工具)

**1 个 mutative**:`produce_message` — 排障场景下没有正当理由发送消息到 kafka,**严禁调用**。
真需要时由用户在 CLI 手动操作(`kafka-console-producer.sh`)。AI 若调用 produce_message,视为越权,
应立刻 abort 并告知用户。binary 本身没有 `--read-only` 或 `--block-tools` flag 可用,本 skill 通过
文档契约约束。

(选 tuannvm 而不是 `@confluentinc/mcp-confluent`:后者依赖 native librdkafka 绑定,
Node ABI 矩阵滞后导致跨平台脆弱;2026-05 实战踩坑后回切纯 Go binary。)

## 连接解析

优先复用：
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul")}}
- `skills/config-executor/scripts/resolve_runtime_from_nacos.py`
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "kuboard"}}
- `skills/config-executor/scripts/kuboard_config.py get`（读取 ConfigMap `.data`；优先用输出的 `runtime`,必要时再看 `content`/`data`）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
- `skills/config-executor/scripts/resolve_runtime_static.py`（从 creds.json 读静态连接串）
{{- end}}

## 诊断命令模板

### 列出所有 topic

```bash
kafka-topics.sh --bootstrap-server $KAFKA_BROKERS --list
```

### 查看 topic 详情

```bash
kafka-topics.sh --bootstrap-server $KAFKA_BROKERS --describe --topic <topic_name>
```

### 查看 consumer group 状态与积压

```bash
kafka-consumer-groups.sh --bootstrap-server $KAFKA_BROKERS --describe --group <group_id>
```

### 消费最新 N 条消息（取证）

```bash
kafka-console-consumer.sh --bootstrap-server $KAFKA_BROKERS \
  --topic <topic_name> --from-latest --max-messages 5 --timeout-ms 10000
```

### 查看死信 / 重试 topic

```bash
kafka-topics.sh --bootstrap-server $KAFKA_BROKERS --list | grep -E "DLQ|RETRY|dead"
```

> kafka CLI 不可用时可用 Python `kafka-python` 包脚本替代(`pip install kafka-python` + 用 `KafkaAdminClient.list_topics()` / `KafkaConsumer.committed()` 等)。

## 输出要求

- 先结论：是否有消费积压/死信/分区不均
- 再证据：
  - consumer-group + topic + partition 对应的 current-offset / log-end-offset / lag
  - 死信 topic 消息条数
  - 最近 N 条消息的 key/value 摘要（脱敏）
- 最后归因：消费端处理慢 / 生产端写入异常 / 分区分配不均 / 网络问题

## 硬约束

- 仅只读诊断；禁止修改 topic 配置、删除 consumer group offset、清除消息。
- 不直接猜测 broker 地址；必须来自 config-executor 的 resolve_runtime 解析结果。
- consumer 查看消息时必须加 `--max-messages` 和 `--timeout-ms` 限制，防止无限消费。


## 自检示例

用户问形如"`<env> 环境的 <具体查询>`"时，按上面的"执行流程"走一遍。

**基础检查**（每次回复必带）：
- 明确列出走了哪些步骤，不要只给最终答案
- 每条结果附来源：env + 连接端（host/port 前缀 + `****` 脱敏）
- 遇到脚本返回 `{"error":..., "hint":...}` → 把 hint 复述给用户，不糊堆栈

**硬约束自检**：
- 用户问写操作（写 / 删 / 改 / drop 等）→ 按 AGENTS.md "数据层写操作被拒"话术拒绝
- 用户给的参数可能拖垮目标（如 `KEYS *` / 全表扫描）→ 先给风险提示，经用户确认后改用更窄范围
