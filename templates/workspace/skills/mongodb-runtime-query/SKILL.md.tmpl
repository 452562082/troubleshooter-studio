---
name: mongodb-runtime-query
description: 通过环境动态查询 MongoDB 数据。先通过 routing 获取环境映射，优先用预注册的 mongodb-<env> mcp tool 直查（已绑好 URI，免手动连接串）；mcp 不可用时 fallback mongosh CLI / Python pymongo，执行只读 query/aggregate/count/listCollections。
---

# MongoDB Runtime Query

## 执行流程（固定）

1. 先确认目标环境与服务；环境映射统一通过 `routing` 获取。
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul") (eq .Infrastructure.PrimaryConfigCenter.Type "kuboard")}}
2. 通过配置中心获取该环境配置文本。
{{- else if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
2. 通过 `resolve_runtime_static.py --agent-id {{.AgentID}} --env <env>` 读取静态连接信息。
{{- end}}
3. 解析配置中的 Mongo 连接信息（优先 `uri`，无则 `hosts`）。
4. **工具选用顺序**：
   - **优先 mcp tool**：本系统按 URI dedup 在每个 env 注册 mongodb MCP。**列工具看注册了几个 source**:
     - 只 1 个 `mongodb-<env>`(单 cluster,典型情况)→ 直接 `tool_use` 调那个。
     - 多个 `mongodb-<source>-<env>`(多 cluster)→ **必须查 routing 的 `references/service-to-datastore-source.yaml`** 拿 `service_routing.data_stores.mongodb.<service>` 对应的 source 段,只调 `mongodb-<对应 source>-<env>`,**禁止瞎选**(跟 grafana datasourceUid 排他契约同款机制)。
   - **mcp 不可用** → fallback **mongosh CLI**(命令简单 + 通用)。
   - **mongosh 也没装** → fallback **Python pymongo**。
5. 执行查询并给出证据：collection、filter/pipeline、结果计数、样例文档关键字段。

## 连接解析

优先复用：
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul")}}
- `skills/config-executor/scripts/resolve_runtime_from_nacos.py`
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "kuboard"}}
- `skills/config-executor/scripts/kuboard_config.py get`（读取 ConfigMap `.data`；优先用输出的 `runtime`,必要时再看 `content`/`data`）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
- `skills/config-executor/scripts/resolve_runtime_static.py`（从 creds.json 读静态连接串）
{{- end}}

## 调用模板

**主路径:mcp tool**(已预注册,首选)

```text
查 mcp tools 列表 → 找名字含 `mongodb-<env>` 的工具。runtime probe 实测 v2.0.2 暴露 8 个:

只读(排障主用):
  query           — 按 filter 查文档(替代 mongosh 的 find)
  aggregate       — pipeline 聚合
  count           — 计数
  listCollections — 列 db 下所有 collection(快速看数据形态)
  serverInfo      — 看 server 版本 / 状态

写工具(SKILL 软约束禁调,**排障不需要**):
  update / insert / createIndex
  注:install 传了 --read-only flag,这 3 个工具调用时会被运行时拦截返错。
  即便没被拦截,排障也不该调它们 — 看到 LLM 调直接视为越权 abort。

连接串已 bake 进 server,不要再传 URI/host/port,只需传 db / collection / filter / pipeline / limit。
```

**fallback 1:mongosh CLI(只读)**

```bash
# count
mongosh "$MONGO_URI" --quiet --eval 'db.users.countDocuments({status: "active"})'

# findOne(限制返回字段)
mongosh "$MONGO_URI" --quiet --eval 'JSON.stringify(db.orders.findOne({order_id: "X123"}, {amount:1, status:1, _id:0}))'

# aggregate
mongosh "$MONGO_URI" --quiet --eval 'JSON.stringify(db.events.aggregate([{$match:{type:"login"}}, {$count:"n"}]).toArray())'
```

**fallback 2:Python pymongo**(mongosh 也没装时)

```bash
python3 -c '
import os, json, pymongo
c = pymongo.MongoClient(os.environ["MONGO_URI"])
print(json.dumps(c.get_default_database().users.find_one({"status":"active"}, {"_id":0}), default=str))
'
```

## 输出要求

- 先结论：是否命中异常（是/否）
- 再证据：collection + 查询条件 + 结果条数 + 关键字段对比
- 最后归因：配置问题 / 数据问题 / 展示问题

## 硬约束

- 仅只读查询；禁止写操作。
- 不直接猜测地址；必须来自 config-executor 的 resolve_runtime 解析结果。


## 自检示例

用户问形如"`<env> 环境的 <具体查询>`"时，按上面的"执行流程"走一遍。

**基础检查**（每次回复必带）：
- 明确列出走了哪些步骤，不要只给最终答案
- 每条结果附来源：env + 连接端（host/port 前缀 + `****` 脱敏）
- 遇到脚本返回 `{"error":..., "hint":...}` → 把 hint 复述给用户，不糊堆栈

**硬约束自检**：
- 用户问写操作（写 / 删 / 改 / drop 等）→ 按 AGENTS.md "数据层写操作被拒"话术拒绝
- 用户给的参数可能拖垮目标（如 `KEYS *` / 全表扫描）→ 先给风险提示，经用户确认后改用更窄范围
