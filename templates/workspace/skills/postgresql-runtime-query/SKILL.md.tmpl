---
name: postgresql-runtime-query
description: 通过环境动态查询 PostgreSQL 数据。先通过 routing 获取环境映射，优先用预注册的 postgresql-<env> mcp tool 直查（已绑好 connection string）；mcp 不可用时 fallback psql CLI / Python psycopg，执行只读 SELECT 查询。
---

# PostgreSQL 运行时查询

## 执行流程（固定）

1. 先确认目标环境与服务；环境映射统一通过 `routing` 获取。
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul") (eq .Infrastructure.PrimaryConfigCenter.Type "kuboard")}}
2. 通过配置中心获取该环境配置文本。
{{- else if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
2. 通过 `resolve_runtime_static.py --agent-id {{.AgentID}} --env <env>` 读取静态连接信息。
{{- end}}
3. 解析配置中的 PostgreSQL 连接信息（host/port/database/user/password 或 JDBC URL）。
4. **工具选用顺序**：
   - **优先 mcp tool**:本系统按 URI dedup 在每个 env 注册 postgresql MCP(@henkey/postgres-mcp-server)。**列工具看注册了几个 source**:
     - 只 1 个 `postgresql-<env>`(单 cluster,典型)→ 直接 `tool_use` 调那个。
     - 多个 `postgresql-<source>-<env>`(多 cluster)→ **必须查 routing 的 `references/service-to-datastore-source.yaml`** 拿 `service_routing.data_stores.postgresql.<service>` 对应 source,只调 `postgresql-<对应 source>-<env>`,**禁止瞎选**。
   - **mcp 不可用** → fallback **psql CLI**(PG 原生客户端,稳定)。
   - **CLI 也没装** → fallback **Python psycopg**。
5. 执行查询并返回证据：表名、SQL、命中行数、关键字段样例。

## 连接解析

优先复用：
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul")}}
- `skills/config-executor/scripts/resolve_runtime_from_nacos.py`
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "kuboard"}}
- `skills/config-executor/scripts/kuboard_config.py get`（读取 ConfigMap `.data`；优先用输出的 `runtime`,必要时再看 `content`/`data`）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
- `skills/config-executor/scripts/resolve_runtime_static.py`（从 creds.json 读静态连接串）
{{- end}}

## 调用模板

**主路径:mcp tool**(已预注册,首选)

```text
查 mcp tools 列表 → 找名字含 `postgresql-<env>` 的工具。@henkey/postgres-mcp-server 提供 17 个工具,排障常用:
  pg_execute_query     — 跑 SELECT(主用,排障查业务数据 / 表状态 / pg_stat_*)
  pg_monitor_database  — 实时指标(连接数 / 锁 / 慢查询 / 大查询)
  pg_debug_database    — 诊断锁 / 慢查询 / 连接问题(高层封装)
  pg_analyze_database  — 性能 + 配置分析(适合开 session 前的总览)
  pg_manage_query      — 查询执行计划 EXPLAIN 等

**排障禁调**(写工具,SKILL 软约束):
  pg_execute_mutation  — INSERT/UPDATE/DELETE/UPSERT
  pg_execute_sql       — 任意 SQL(可含写)

连接串已 bake 到 server env,不需手动 host/port/user/pass/db。
PG 端建议给只读 role 兜底,即便 LLM 误调 mutation 也会被 PG 拒(从 mcp 软约束 → PG 端硬约束)。
```

**fallback 1:psql CLI(只读)**

```bash
PGPASSWORD="$PG_PASS" psql -h "$PG_HOST" -p "$PG_PORT" -U "$PG_USER" -d "$PG_DB" \
  -c "SET default_transaction_read_only = on; SELECT order_id, status FROM orders WHERE status='pending' LIMIT 20;"
```

**fallback 2:Python psycopg**(psql 也没装时)

```bash
python3 -c '
import os, psycopg2, json
c = psycopg2.connect(host=os.environ["PG_HOST"], port=os.environ["PG_PORT"],
                     user=os.environ["PG_USER"], password=os.environ["PG_PASS"],
                     dbname=os.environ["PG_DB"])
cur = c.cursor()
cur.execute("SELECT order_id, status FROM orders WHERE status=%s LIMIT 20", ("pending",))
print(json.dumps(cur.fetchall(), default=str))
'
```

### 常用诊断 SQL

```sql
-- 活跃连接
SELECT pid, usename, state, query_start, query FROM pg_stat_activity WHERE state = 'active';
-- 表大小
SELECT relname, pg_size_pretty(pg_total_relation_size(oid)) FROM pg_class WHERE relkind = 'r' ORDER BY pg_total_relation_size(oid) DESC LIMIT 10;
-- 慢查询（需 pg_stat_statements 扩展）
SELECT query, calls, mean_exec_time, total_exec_time FROM pg_stat_statements ORDER BY mean_exec_time DESC LIMIT 10;
```

## 输出要求

- 先结论：是否命中异常（是/否）
- 再证据：表名 + SQL + 命中行数 + 关键字段
- 最后归因：数据问题 / 配置问题 / 索引/性能问题

## 硬约束

- 仅只读查询（SELECT）；禁止 INSERT/UPDATE/DELETE/DDL。
- 不直接猜测地址；必须来自 config-executor 的 resolve_runtime 解析结果。
- 涉及敏感数据时做脱敏处理（手机号/身份证/密码字段）。


## 自检示例

用户问形如"`<env> 环境的 <具体查询>`"时，按上面的"执行流程"走一遍。

**基础检查**（每次回复必带）：
- 明确列出走了哪些步骤，不要只给最终答案
- 每条结果附来源：env + 连接端（host/port 前缀 + `****` 脱敏）
- 遇到脚本返回 `{"error":..., "hint":...}` → 把 hint 复述给用户，不糊堆栈

**硬约束自检**：
- 用户问写操作（写 / 删 / 改 / drop 等）→ 按 AGENTS.md "数据层写操作被拒"话术拒绝
- 用户给的参数可能拖垮目标（如 `KEYS *` / 全表扫描）→ 先给风险提示，经用户确认后改用更窄范围
