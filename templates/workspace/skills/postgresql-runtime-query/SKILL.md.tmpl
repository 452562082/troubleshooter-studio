---
name: postgresql-runtime-query
description: 通过环境动态查询 PostgreSQL 数据。先通过 routing 获取环境映射，再按配置源读取并解析 PG 连接信息，最后通过 mcporter 临时拉起 MySQL MCP（兼容 PG）或 psql CLI 执行只读查询。
---

# PostgreSQL Runtime Query

## 执行流程（固定）

1. 先确认目标环境与服务；环境映射统一通过 `routing` 获取。
{{- if or (eq .Infrastructure.ConfigCenter.Type "nacos") (eq .Infrastructure.ConfigCenter.Type "apollo") (eq .Infrastructure.ConfigCenter.Type "consul")}}
2. 通过配置中心获取该环境配置文本。
{{- else if eq .Infrastructure.ConfigCenter.Type "env-vars"}}
2. 通过 `resolve_runtime_static.py --agent-id {{.AgentID}} --env <env>` 读取静态连接信息。
{{- else if eq .Infrastructure.ConfigCenter.Type "kubernetes"}}
2. 通过 `resolve_runtime_k8s.py --agent-id {{.AgentID}} --env <env>` 从 K8s ConfigMap/Secret 解析连接信息。
{{- end}}
3. 解析配置中的 PostgreSQL 连接信息（host/port/database/user/password 或 JDBC URL）。
4. 优先用 mcporter 临时启动 MCP（只读），备选 psql CLI。
5. 执行查询并返回证据：表名、SQL、命中行数、关键字段样例。

## 连接解析

优先复用：
{{- if or (eq .Infrastructure.ConfigCenter.Type "nacos") (eq .Infrastructure.ConfigCenter.Type "apollo") (eq .Infrastructure.ConfigCenter.Type "consul")}}
- `skills/config-executor/scripts/resolve_runtime_from_nacos.py`
{{- end}}
{{- if eq .Infrastructure.ConfigCenter.Type "env-vars"}}
- `skills/config-executor/scripts/resolve_runtime_static.py`（从 creds.json 读静态连接串）
{{- end}}
{{- if eq .Infrastructure.ConfigCenter.Type "kubernetes"}}
- `skills/config-executor/scripts/resolve_runtime_k8s.py`（从 K8s ConfigMap/Secret 解析）
{{- end}}

## mcporter 调用模板（只读）

```bash
mcporter list \
  --stdio "npx" \
  --stdio-arg "-y" \
  --stdio-arg "@benborla29/mcp-server-mysql" \
  --env "MYSQL_HOST=$PG_HOST" \
  --env "MYSQL_PORT=$PG_PORT" \
  --env "MYSQL_USER=$PG_USER" \
  --env "MYSQL_PASS=$PG_PASS" \
  --env "MYSQL_DB=$PG_DB" \
  --schema
```

## psql CLI 备选

```bash
PGPASSWORD=$PG_PASS psql -h $PG_HOST -p $PG_PORT -U $PG_USER -d $PG_DB \
  -c "SET default_transaction_read_only = on; SELECT * FROM <table> WHERE <condition> LIMIT 50;"
```

### 常用诊断 SQL

```sql
-- 活跃连接
SELECT pid, usename, state, query_start, query FROM pg_stat_activity WHERE state = 'active';
-- 表大小
SELECT relname, pg_size_pretty(pg_total_relation_size(oid)) FROM pg_class WHERE relkind = 'r' ORDER BY pg_total_relation_size(oid) DESC LIMIT 10;
-- 慢查询（需 pg_stat_statements 扩展）
SELECT query, calls, mean_exec_time, total_exec_time FROM pg_stat_statements ORDER BY mean_exec_time DESC LIMIT 10;
```

## 输出要求

- 先结论：是否命中异常（是/否）
- 再证据：表名 + SQL + 命中行数 + 关键字段
- 最后归因：数据问题 / 配置问题 / 索引/性能问题

## 硬约束

- 仅只读查询（SELECT）；禁止 INSERT/UPDATE/DELETE/DDL。
- 不直接猜测地址；必须来自 config-executor 的 resolve_runtime 解析结果。
- 涉及敏感数据时做脱敏处理（手机号/身份证/密码字段）。
