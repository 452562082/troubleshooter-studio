---
name: rabbitmq-runtime-query
description: 通过 RabbitMQ HTTP Management API(端口 15672)查询队列状态。先通过 routing 获取环境映射,凭据从 install creds 读,curl + Basic Auth 查队列长度 / 消费者 / exchange / 节点健康等。2026-05-15 起 mcp 主路径暂禁(PyPI 包实测两个都 broken,详见 install_native_mcp_common.go::buildRabbitMQ)。
---

# RabbitMQ 运行时查询

## 执行流程（固定）

1. 先确认目标环境与服务；环境映射统一通过 `routing` 获取。
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul") (eq .Infrastructure.PrimaryConfigCenter.Type "kuboard")}}
2. 通过配置中心获取该环境配置文本。
{{- else if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
2. 通过 `resolve_runtime_static.py --agent-id {{.AgentID}} --env <env>` 读取静态连接信息。
{{- end}}
3. 解析配置中的 RabbitMQ Management 连接信息(URL / user / password)。
4. **工具选用顺序**(2026-05-15 起 mcp 主路径暂禁):
   - **主路径:curl + HTTP Management API**(端口 15672,自带 Basic Auth) — RabbitMQ 团队官方维护,极稳定,所有查询场景都覆盖。凭据从 install creds 的 `RABBITMQ_USER_<ENV>` / `RABBITMQ_PASS_<ENV>` 读。
   - **可选 fallback:`rabbitmqadmin` CLI** — RabbitMQ 标准发行版自带,本质也是 HTTP API,curl 不顺手时备用。
   - **为什么不走 mcp**:实测 PyPI 上 `amq-mcp-server-rabbitmq`(amazon-mq) 源码引用 fastmcp 不存在的 `BearerAuthProvider`,装上必挂;`rabbitmq-mcp-server`(社区) 依赖声明缺一堆。等社区出能跑通的包,主路径会切回 mcp,详见 install_native_mcp_common.go::buildRabbitMQ 注释。
5. 返回证据:队列状态、消息堆积量、消费者数量、exchange 绑定关系。

## 连接解析

优先复用：
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul")}}
- `skills/config-executor/scripts/resolve_runtime_from_nacos.py`
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "kuboard"}}
- `skills/config-executor/scripts/kuboard_config.py get`（读取 ConfigMap `.data`；优先用输出的 `runtime`,必要时再看 `content`/`data`）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
- `skills/config-executor/scripts/resolve_runtime_static.py`（从 creds.json 读静态连接串）
{{- end}}

## 诊断命令模板

### 列出所有队列及消息数量

```bash
curl -s -u $RABBITMQ_USER:$RABBITMQ_PASS "$RABBITMQ_URL/api/queues?columns=name,messages,messages_ready,messages_unacknowledged,consumers" | python3 -m json.tool
```

### 查看单个队列详情

```bash
curl -s -u $RABBITMQ_USER:$RABBITMQ_PASS "$RABBITMQ_URL/api/queues/%2F/<queue_name>" | python3 -m json.tool
```

### 列出所有 exchange

```bash
curl -s -u $RABBITMQ_USER:$RABBITMQ_PASS "$RABBITMQ_URL/api/exchanges?columns=name,type,durable" | python3 -m json.tool
```

### 查看 exchange 绑定

```bash
curl -s -u $RABBITMQ_USER:$RABBITMQ_PASS "$RABBITMQ_URL/api/exchanges/%2F/<exchange_name>/bindings/source" | python3 -m json.tool
```

### 列出所有消费者

```bash
curl -s -u $RABBITMQ_USER:$RABBITMQ_PASS "$RABBITMQ_URL/api/consumers" | python3 -c "import sys,json; [print(f\"{c['queue']['name']} -> {c['consumer_tag']}\") for c in json.load(sys.stdin)]"
```

### 集群节点健康检查

```bash
curl -s -u $RABBITMQ_USER:$RABBITMQ_PASS "$RABBITMQ_URL/api/nodes" | python3 -c "import sys,json; [print(f\"{n['name']}: running={n['running']}, mem={n['mem_used']//1048576}MB\") for n in json.load(sys.stdin)]"
```

## 输出要求

- 先结论：是否有消息堆积/消费者掉线/节点异常
- 再证据：
  - 队列名 + messages_ready / messages_unacknowledged / consumers 数
  - 堆积最严重的 TOP 5 队列
  - 死信队列消息条数（若配置了 DLX）
- 最后归因：消费端处理慢 / 生产端写入异常 / 绑定配置错误 / 节点故障

## 硬约束

- 仅只读诊断；禁止 purge queue、delete queue/exchange、publish 消息。
- 不直接猜测地址；必须来自 config-executor 的 resolve_runtime 解析结果。
- URL 中 vhost 默认为 `%2F`（即 `/`），如有自定义 vhost 需从配置中获取。


## 自检示例

用户问形如"`<env> 环境的 <具体查询>`"时，按上面的"执行流程"走一遍。

**基础检查**（每次回复必带）：
- 明确列出走了哪些步骤，不要只给最终答案
- 每条结果附来源：env + 连接端（host/port 前缀 + `****` 脱敏）
- 遇到脚本返回 `{"error":..., "hint":...}` → 把 hint 复述给用户，不糊堆栈

**硬约束自检**：
- 用户问写操作（写 / 删 / 改 / drop 等）→ 按 AGENTS.md "数据层写操作被拒"话术拒绝
- 用户给的参数可能拖垮目标（如 `KEYS *` / 全表扫描）→ 先给风险提示，经用户确认后改用更窄范围
