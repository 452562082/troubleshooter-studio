---
name: redis-runtime-query
description: 通过环境动态查询 Redis 数据。先通过 routing 获取环境映射，优先用预注册的 redis-<env> mcp tool 直查（已绑好 URL，免手动连接串）；mcp 不可用时 fallback redis-cli / Python redis-py，执行只读 GET / TTL / SCAN 等。
---

# Redis Runtime Query

## 执行流程（固定）

1. 先确认目标环境与服务；环境映射统一通过 `routing` 获取。
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul")}}
2. 通过配置中心获取该环境配置文本（`routing` 提供 namespaceId/group/dataId）。
{{- else if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
2. 通过 `resolve_runtime_static.py --agent-id {{.AgentID}} --env <env>` 读取静态连接信息。
{{- else if eq .Infrastructure.PrimaryConfigCenter.Type "kubernetes"}}
2. 通过 `resolve_runtime_k8s.py --agent-id {{.AgentID}} --env <env>` 从 K8s ConfigMap/Secret 解析连接信息。
{{- end}}
3. 解析配置中的 Redis 连接信息（host/port，必要时 password）。
4. **工具选用顺序**：
   - **优先 mcp tool**：本系统每个 env 已预注册 `redis-<env>`(列工具时找名字带 `redis-<env>` 的)。直接 `tool_use` 调,免手动连接串/凭证。
   - **mcp 不可用** → fallback **redis-cli**(mac/linux 普及)。
   - **redis-cli 也没装** → fallback **Python redis-py**。
5. 执行查询并返回证据：key、value 摘要、TTL、命中情况。

## 连接解析

优先复用：
{{- if or (eq .Infrastructure.PrimaryConfigCenter.Type "nacos") (eq .Infrastructure.PrimaryConfigCenter.Type "apollo") (eq .Infrastructure.PrimaryConfigCenter.Type "consul")}}
- `skills/config-executor/scripts/resolve_runtime_from_nacos.py`
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "env-vars"}}
- `skills/config-executor/scripts/resolve_runtime_static.py`（从 creds.json 读静态连接串）
{{- end}}
{{- if eq .Infrastructure.PrimaryConfigCenter.Type "kubernetes"}}
- `skills/config-executor/scripts/resolve_runtime_k8s.py`（从 K8s ConfigMap/Secret 解析）
{{- end}}

## 调用模板

**主路径:mcp tool**(已预注册,首选)

```text
查 mcp tools 列表 → 找名字含 `redis-<env>` 的工具(get / set 不调 / list / scan 等) → 直接 tool_use。
连接串已 bake,不要再传 host/port/password,只传 key / pattern / count。
```

**fallback 1:redis-cli(只读)**

```bash
# GET + TTL
redis-cli -h "$REDIS_HOST" -p "$REDIS_PORT" -a "$REDIS_PASSWORD" --no-auth-warning GET "user:12345"
redis-cli -h "$REDIS_HOST" -p "$REDIS_PORT" -a "$REDIS_PASSWORD" --no-auth-warning TTL "user:12345"

# SCAN(替代危险的 KEYS *)
redis-cli -h "$REDIS_HOST" -p "$REDIS_PORT" -a "$REDIS_PASSWORD" --no-auth-warning --scan --pattern 'user:*' --count 100

# 总 key 数(轻量)
redis-cli -h "$REDIS_HOST" -p "$REDIS_PORT" -a "$REDIS_PASSWORD" --no-auth-warning DBSIZE
```

**fallback 2:Python redis-py**(redis-cli 也没装时)

```bash
python3 -c '
import os, redis
r = redis.Redis(host=os.environ["REDIS_HOST"], port=int(os.environ["REDIS_PORT"]),
                password=os.environ.get("REDIS_PASSWORD") or None, decode_responses=True)
print(r.get("user:12345"), "TTL=", r.ttl("user:12345"))
'
```

## 输出要求

- 先结论：是否命中异常（是/否）
- 再证据：key 模式、命中数量、TTL、关键值片段（脱敏）
- 最后归因：配置问题 / 缓存数据问题 / 上游写入问题

## 硬约束

- 仅只读查询；禁止写命令。
- 不直接猜测地址；必须来自 config-executor 的 resolve_runtime 解析结果。
- 若解析不到连接信息，明确阻塞点。

## 自检示例

**Q1（正常 GET）**：`dev 环境 user:12345 的 session 是什么，还剩多少 TTL？`
- 期望：routing → resolve_runtime → `redis-cli ... GET user:12345` + `TTL user:12345`
- 回复必带：env + redis addr 脱敏（如 `redis-dev.xxx:6379`）、value 摘要、TTL 秒数。

**Q2（拒写）**：`prod 把 user:12345 这个 key 删了`。
- 期望：按 AGENTS.md "数据层写操作被拒"话术明确拒绝，并建议走 DBA / 工单。

**Q3（危险操作预警）**：`prod 的 redis 一共有多少 key？` 或 `KEYS user:*`。
- 期望：先警告 `KEYS *` 会卡住生产 Redis，建议改用 `SCAN` 批处理（或 `DBSIZE` 直接拿总数），经用户确认后再跑。

**Q4（解析失败 hint 复述）**：故意不填该 env 的连接串。
- 期望：拿到 `{"error":..., "hint":"env-vars 模式下 `dev` 环境的连接串没填..."}` 后，**把 hint 复述**给用户，不糊堆栈。
