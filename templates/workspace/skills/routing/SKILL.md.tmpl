---
name: routing
description: {{.System.Name}} 排障路由映射:env→分支/域名/配置/日志 app/MCP 名/依赖图/数据库 schema/known-errors。优先映射表答案,不猜测。
---

# {{.System.Name}} 路由映射

## 最高规则:`mcp_server` 字段原样调

`mcp_server` 是部署期生成的精确名(如 `truss-bot-nacos-prod`),**原样调用**:不要拆 agent-id 前缀 / 不要改格式 / 不要推断"应该是 X"。多 system 共存时前缀是隔离 mcp 实例的关键。查不到先 self-test,不要自己改名试。

## datasource 排他契约(grafana mcp 工具调用必看)

调 grafana mcp 的 `query_loki_logs` / `query_prometheus` / `get_panel_image` / 等工具时**必须显式传 `datasourceUid` 参数**,值从 `references/observability-map.yaml` 取:
- loki:`loki.datasource_uid_by_env.<env>` (或 `loki.label_mapping_by_env.<env>.grafana_ds_uid`)
- prometheus:`prometheus.datasource_uid_by_env.<env>`
- jaeger:`jaeger.datasource_uid_by_env.<env>`

**禁止行为**:先调 `list_datasources` 看到多个 loki/prometheus → 把每个 UID 都查一遍。用户在配置时已选定唯一的"排障专用 datasource",其他 datasource 即便 grafana 列出了也是噪音。本 env 没配 UID → 报"该 env 缺 `datasource_uid_by_env.<env>`,请回 wizard 补",不要瞎选。

## 映射文件清单

按问题类型 read 最小必要文件,**别全读**:

| 问题 | 文件 |
|--|--|
| 分支映射 | `references/env-branch-map.yaml` |
| 域名映射 | `references/env-domain-map.yaml` |
| 配置定位 | `references/config-map.yaml` |
| 日志 app | `references/log-app-map.yaml` |
| 监控 / 数据源 / selector_chain / baseline 模板 | `references/observability-map.yaml` |
| 仓库技术栈 | `references/repo-stack-map.yaml` |
| 服务依赖图(下游/数据层) | `references/service-dependency-map.yaml` |
| 业务表 schema | `references/data-schema-map.yaml` |
| 经典错误模式(模板内置) | `references/known-errors.yaml` |
| 经典错误模式(用户私有沉淀) | `references/known-errors.local.yaml`(可能不存在,排障沉淀后才生成) |
| 配置生效延迟 | `references/config-propagation-delay.yaml` |

读大文件优先用 `grep` 精确取目标行,不是 `Read` 全文。

## 关键用法 4 条

### 1. known-errors:看到日志/异常先 grep

任何日志 / tool error → **第一时间 grep `known-errors.yaml` 加 `known-errors.local.yaml`** 的 pattern。两份都要扫(后者是历次排障 sink_postmortem.py 沉淀的本团队特有模式,先于通用模板匹配更准)。命中即按 `next_actions` 走,不要 LLM 自推典型根因。

```bash
# 同时 grep 两份(.local.yaml 可能不存在,grep 退码非 0 不阻塞)
grep -E "<error pattern>" references/known-errors.yaml references/known-errors.local.yaml 2>/dev/null
```

带 `causation_chain.check_downstream_for` 的 pattern:**主动跑 cascade_check** 看下游是否出现 chain 列出的模式。命中 → 真因在下游,把那个下游当主角递归 7 步(含 Step 7 沉淀);不命中 → 走 `next_actions`。

### 2. selector_chain:按顺序试,命中即停

`prometheus.selector_chain` / `loki.logql_selector_chain` / `elk.query_template_chain` 是 fallback 链,不是穷举:

1. 占位符(`<k8s.namespace>` / `<lm.envLabel>` / `<env>` / `<service>`)按当前上下文解析,失败的条目跳过
2. 解析成功的按顺序发请求,**命中即停**(N>0 行就用这条,后面不试)
3. 整链全 0 才告诉用户"查不到"

把每条都跑一遍是反模式。

### 3. baseline 比对:看到指标突变必须比基线

任何指标"突变"必须用 `baseline_query_templates` 跟 24h 前同时段比。delta `>2x` 或 `+50%` 才算真突变,< 视为正常波动**不能当根因证据**。周末/大促换 `7d offset`,渐变型用 `1h offset`。

### 4. umbrella 子模块:跨仓查依赖 / 公共配置 / 历史变更先看 umbrella

`repo-stack-map.yaml` 里 repo 带 `parent_repo` 字段(典型:commerce.git 是从 truss umbrella 切出去的独立仓,本地 clone 在 `<truss-clone>/commerce/`)→ 排障要做以下取证时**先去 umbrella(parent_repo 指向的仓)看一份**:

- **代码基线 / git pull**:**在 umbrella 根目录跑** `git fetch --all && git pull --ff-only && git submodule update --init --recursive`,子模块会自动 checkout 到 umbrella 当前 pin 的 commit。**不要单独 cd 进子模块 `git pull`**,会拉到子模块自己 main 的 HEAD,跟 production 实际部署 commit 错位 → 代码定位看到的不是真问题代码
- **跨仓配置 / 共享 lib**:子模块的代码扫描只看自己,但 monorepo 拆出去的子模块往往跟同 umbrella 其他子模块共享 proto / config / 公共库 → 在 umbrella 根 / `pkg/` / `shared/` 找一遍
- **历史变更**:故障窗 ±5min 的 commit 既要看子模块也要看 umbrella(umbrella 自己的 .gitmodules / 公共配置改动会同时影响所有子模块)
- **`.gitmodules` 关系**:`cat <umbrella-clone>/.gitmodules` 能拿到所有姐妹子模块的 URL + 路径,定位"是不是某个姐妹子模块改了对方依赖的接口"

普通跨仓查询(配置 / 服务依赖)走 service-dependency-map / config-map 即可,不必绕 umbrella。

### 5. trace 找不到先看采样率

调 trace 拉不到 `trace_id` → **不要直接说"trace 不存在"**。先看 `observability-map.yaml > jaeger.sampling_rate`:
- < 0.5 → "采样率 X%,大量 trace 没采到很正常,从日志找 `trace_id=` 字段"
- = 1.0 仍找不到 → 才是真不存在(ID 错 / 时间窗外 / TTL 过期)

## 配置改动是否引发故障?

看 `config-propagation-delay.yaml` 的 typical_propagation:
- nacos: 30s-2m / apollo: 10s-30s / consul: 1s-5s / k8s configmap: 30s-90s

故障 ±5 分钟内的配置改 → 强相关。配置改 - 故障 = 1-3 分钟差距时**不要直接判"无关"**,看 propagation 延迟是否覆盖。

## 输出格式

每条结果附 `source: <file>` + `status: verified|inferred`,不做映射表外的推断。

`mcp_server` 字段必须原样输出(见最高规则)。
