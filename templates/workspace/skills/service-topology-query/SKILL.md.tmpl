---
name: service-topology-query
description: 从 routing 生成的静态服务拓扑按失败 URL、method/path 或 service 查询有界下游路径，并携带 endpoint 证据供 trace/log/runtime 逐跳验证。
---

# 服务拓扑查询

本 skill 只读取：

- `skills/routing/references/service-topology.yaml`：正式的 automatic / confirmed / manual 服务边。
- `skills/routing/references/endpoint-evidence.yaml`：端点位置、匹配理由以及 candidate / stale 等证据状态。

拓扑只作为导航证据，不能单独证明根因。它回答“下一步应验证哪些服务、端点和仓库”，不回答“根因已经在哪”。如果静态拓扑和当前环境的运行时 trace 冲突，以 trace 为准，并提示拓扑可能过期、需要重新分析。

## 查询

从 workspace 根执行。已知入口服务和失败请求时：

```bash
python3 skills/service-topology-query/scripts/query.py \
  --service <service> --method <HTTP-method> --path <failed-URL-or-path> \
  --max-depth 3 --json
```

只有失败 URL、尚未确定入口服务时可省略 `--service`：

```bash
python3 skills/service-topology-query/scripts/query.py \
  --method <HTTP-method> --path <failed-URL-or-path> --json
```

查询完整限定名的 gRPC 方法时：

```bash
python3 skills/service-topology-query/scripts/query.py \
  --service <service> --protocol grpc \
  --rpc-method </package.Service/Method> --json
```

`--protocol` 会限制首跳协议；提供 method/path 时默认 HTTP，提供 `--rpc-method` 时默认 gRPC。gRPC 方法比较会统一去掉可选的前导 `/`，但仍要求完整的 `package.Service/Method` 精确匹配。

`--max-depth` 默认 3，脚本强制限制在 1..5。路径会去掉 URL query/fragment；普通的 `:id`、`{id}`、`[id]` 统一为 `{param}` 且只匹配一个 segment，`*path`、`:path*`、`{*path}`、`[...path]` 统一为 `{wildcard}` 且匹配一个或多个 segment。遍历检测环，只返回无环的完整叶路径或深度边界路径。

稳定 JSON 字段：

- `status`：`ok`、`no_match` 或 `unavailable`。
- `query`：归一化后的 `service/protocol/method/path/rpc_method/max_depth`。
- `paths`：按人工确认关系优先、路径最低置信度、路径长度和服务名字典序稳定排序。
- `warnings`：缺文件、格式错误、环、candidate / stale 标签等导航风险。
- `fallback`：无正式路径时为 `routing_rg_read`；成功时为 `null`。

带 method/path 查询时，首跳只返回实际命中的 route；首跳 `status/confidence/evidence` 也只来自这些 route 对应的 endpoint edge，不受同一服务对上其它路由影响。拓扑或 endpoint evidence 缺失或格式错误、route 引用找不到证据时返回 `status=unavailable`、空 `paths` 和 `routing_rg_read` fallback，不伪造空/null 证据。

## 逐跳验证

1. 先取排序靠前的正式路径，但多候选无法消歧时保留全部候选，不任选一个写成事实。
2. 每一跳优先用当前环境 trace 验证实际调用；无 trace 时查同时间窗的网关/服务日志、request id，再查 K8s/runtime 状态。
3. 输出时明确标注：`运行时已验证`、`静态正式拓扑`、`candidate 待确认`、`stale 可能过期`。candidate / stale 只能用于继续取证，不能冒充正式依赖。
4. 选中具体仓库和 endpoint 后，如果生成物包含 `code-intelligence-query`，先 Read 该 skill 并按其分支/freshness 规则使用 CodeGraph；不可用或失败时回退 `rg` + Read 源码。
5. `status=no_match|unavailable` 或 `fallback=routing_rg_read` 时，回到 routing 的 repo/service 映射、`service-dependency-map.yaml` 与 `rg`/Read，不阻塞主排障流程。

## 边界

- 只读，不修改拓扑、override、仓库或运行时状态；不执行外部网络探测。
- 当前拓扑只描述已建模的 HTTP/gRPC 同步调用，不覆盖 Kafka topic、RabbitMQ queue/exchange 等异步事件拓扑，也不得声称具备异步事件覆盖。
- 默认只向下游查三跳。只有新增运行时证据明确指向更深层时，才可在 5 跳硬上限内扩大一次查询。
