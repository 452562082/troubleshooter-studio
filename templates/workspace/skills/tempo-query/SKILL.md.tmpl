---
name: tempo-query
description: 通过 Grafana Tempo HTTP API 查询分布式链路追踪。按 trace ID 或 tag 搜索 trace，通常经 Grafana datasource 或直接访问 Tempo API，定位跨服务延迟与错误传播路径。
---

# Tempo Query（Grafana Tempo）

## 何时使用

- 用户反馈接口慢 / 超时 / 报错，需要看请求在各服务间的流转
- 有 trace ID（从日志或响应头中获取）需要展开查看
- 需要按 tag（service.name、http.status_code 等）搜索相关 trace

## 执行流程

1. 确认 env（通过 `routing`）。
2. 获取 Tempo URL：
{{- range .Environments}}
   - `{{.ID}}` → config 中声明的 Tempo / Grafana URL
{{- end}}
3. 按场景选择查询方式：
   - **有 trace ID**：直接查询 → `/api/traces/{traceID}`
   - **无 trace ID**：按 TraceQL 搜索 → `/api/search`
4. 分析 span 树：找到耗时最长的 span、错误 span、跨服务调用链。

## Tempo HTTP API

### 按 trace ID 查询

```bash
curl -s "$TEMPO_URL/api/traces/<traceID>" \
  -H "Accept: application/json" \
  | python3 -m json.tool
```

### 通过 Grafana Datasource 查询 trace

```bash
curl -s "$GRAFANA_URL/api/datasources/proxy/uid/<tempo_ds_uid>/api/traces/<traceID>" \
  -H "Authorization: Bearer $GRAFANA_TOKEN" \
  | python3 -m json.tool
```

### 按 tag 搜索 trace（TraceQL）

```bash
curl -s "$TEMPO_URL/api/search" \
  --data-urlencode "q={ resource.service.name=\"<service>\" && status=error }" \
  --data-urlencode "limit=20" \
  --data-urlencode "start=$(date -d '1 hour ago' +%s)" \
  --data-urlencode "end=$(date +%s)" \
  | python3 -m json.tool
```

### 按 tag 搜索（简单模式）

```bash
curl -s "$TEMPO_URL/api/search?tags=service.name%3D<service>&limit=20&start=$(date -d '1 hour ago' +%s)&end=$(date +%s)" \
  | python3 -m json.tool
```

### 查看 tag 可选值

```bash
curl -s "$TEMPO_URL/api/search/tags" | python3 -m json.tool
curl -s "$TEMPO_URL/api/search/tag/service.name/values" | python3 -m json.tool
```

## 输出要求

- 先结论：链路是否异常（是/否）
- 再证据：
  - trace ID + span 数量 + 总耗时
  - 耗时 TOP 3 span（service + operation + duration）
  - 错误 span（若有：service + status + attributes）
- 最后归因：
  - 哪个服务是瓶颈
  - 是网络延迟 / 下游超时 / 自身处理慢
  - 建议排查方向

## 跨 skill 协作（找到嫌疑 span 后必走）

trace 给的是"哪一段慢/错"，**真正根因**通常要再下钻一层。拿到嫌疑 span 后**主动**继续:

1. **嫌疑 span 的 service + 时间窗 → Loki 拉错误日志**
   - 走 routing `observability.loki.logql_selector_chain`,优先用 wizard 配过的 `(env_label, service_label)` 命中
   - 时间范围:trace start ± 30s,避免遗漏前后关联日志
{{- if hasSkill . "k8s-runtime-query" }}
2. **嫌疑 span 的 service + 时间窗 → KuboardGetPodLogs / KuboardListEvents**
   - 该 service 对应的 K8s workload(routing `observability.k8s_runtime.service_map[env+service]` 拿 cluster/namespace/label_selector)
   - 看 trace 时间点附近有没有 OOMKilled / CrashLoopBackOff / 突发重启
{{- end}}
{{- if .Infrastructure.Observability.Prometheus.Enabled }}
3. **嫌疑 span 的 service + 时间窗 → PromQL 同窗口指标**
   - 走 routing `observability.prometheus.selector_chain` 拼 selector
   - 关键指标:`rate(http_requests_total{...}[1m])` 看 QPS / `histogram_quantile(0.99, ...)` 看 p99 / 错误率 突变
{{- end}}
4. **该 service 最近 1h 变更** —— K8s rollout history、配置中心 history、git log 同时看一遍

**反模式**:trace 看完就停在"OK 是 X 服务慢"。这只是症状,根因是 X 服务的 pod / 配置 / 代码哪个出了问题,必须再走一步上面的协作链才能给出可处置的建议。

## 硬约束

- 仅只读查询；不修改 trace 数据。
- 必须先通过 `routing` 确认 env，再用对应 env 的 Tempo/Grafana URL。
- 展示 span 详情时对敏感 attribute（token、password）做脱敏。
- 若通过 Grafana 代理访问，需提供有效 Bearer token。


## 自检示例

用户问形如"`<env> 环境的 <具体查询>`"时，按上面的"执行流程"走一遍。

**基础检查**（每次回复必带）：
- 明确列出走了哪些步骤，不要只给最终答案
- 每条结果附来源：env + 连接端（host/port 前缀 + `****` 脱敏）
- 遇到脚本返回 `{"error":..., "hint":...}` → 把 hint 复述给用户，不糊堆栈

**硬约束自检**：
- 用户问写操作（写 / 删 / 改 / drop 等）→ 按 AGENTS.md "数据层写操作被拒"话术拒绝
- 用户给的参数可能拖垮目标（如 `KEYS *` / 全表扫描）→ 先给风险提示，经用户确认后改用更窄范围
