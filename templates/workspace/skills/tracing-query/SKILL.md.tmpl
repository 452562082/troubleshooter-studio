---
name: tracing-query
description: 通过 Jaeger 查询分布式链路追踪。优先用预注册的 jaeger-<env> mcp tool(opentelemetry-mcp,免手填 URL),mcp 不可用时 fallback curl + Jaeger HTTP API。按 trace ID、service name、时间范围查询 span,定位跨服务延迟与错误传播路径。排障时的核心取证手段。
---

# Tracing Query（Jaeger）

## 何时使用

- 用户反馈接口慢 / 超时 / 报错，需要看请求在各服务间的流转
- 有 trace ID（从日志或响应头中获取）需要展开查看
- 需要对比正常请求与异常请求的 span 差异

## 执行流程

1. 确认 env(通过 `routing`)。
2. **工具选用顺序**:
   - **优先 mcp tool**:本系统每个 env 已预注册 `jaeger-<env>`(opentelemetry-mcp,uvx 启动;列工具时找名字带 `jaeger-<env>` 的)。直接 `tool_use` 调,免手填 JAEGER_URL/拼 HTTP API。
   - **mcp 不可用** → fallback **curl + Jaeger HTTP API**(JAEGER_URL 从 routing `observability.jaeger.url_by_env.<env>` 取)。
3. 按场景选择查询方式:
   - **有 trace ID**:直接查
   - **无 trace ID**:按 service + operation + 时间范围搜索
4. 分析 span 树:找到耗时最长的 span、错误 span、跨服务调用链。

## 主路径:mcp tool(已预注册,首选)

```text
查 mcp tools 列表 → 找名字含 `jaeger-<env>` 的工具。traceloop/opentelemetry-mcp 暴露 5 个:
  search_traces  — 按 service / operation / 时间范围 / duration 搜索 trace
  get_trace      — 按 trace_id 取完整 trace
  list_services  — 列所有 service
  find_errors    — 找出错误的 trace(排障最常用之一)
  get_llm_usage  — LLM 调用统计(本工具偏 LLM-tracing 设计,普通微服务排障**用不上**)

URL 已 bake 进 server,只传 trace_id / service / operation / time-range,不需手动 JAEGER_URL。
```

> **说明**:traceloop/opentelemetry-mcp 是 LLM-tracing 公司做的,工具签名带不少 `gen_ai_system` /
> `gen_ai_model` 等 LLM 专用 filter — 普通微服务排障时**全部留空即可**,不影响通用 trace 查询。
> `get_llm_usage` 这条工具只在排查 AI 应用 token / 成本时用,常规排障忽略。

## fallback:Jaeger HTTP API

### 按 trace ID 查询

```bash
curl "$JAEGER_URL/api/traces/<traceID>" | python3 -m json.tool
```

### 按 service + 时间范围搜索

```bash
curl "$JAEGER_URL/api/traces?service=<service>&start=<epoch_us>&end=<epoch_us>&limit=20"
```

### 列出所有 service

```bash
curl "$JAEGER_URL/api/services"
```

### 查看某 service 的 operation 列表

```bash
curl "$JAEGER_URL/api/services/<service>/operations"
```

> 时间戳单位：微秒（epoch × 1000000）

## 输出要求

- 先结论：链路是否异常（是/否）
- 再证据：
  - trace ID
  - span 数量 + 总耗时
  - 耗时 TOP 3 span（service + operation + duration）
  - 错误 span（若有：service + error tag + 日志）
- 最后归因：
  - 哪个服务是瓶颈
  - 是网络延迟 / 下游超时 / 自身处理慢
  - 建议排查方向

## 跨 skill 协作（找到嫌疑 span 后必走）

trace 给的是"哪一段慢/错"，**真正根因**通常要再下钻一层。拿到嫌疑 span 后**主动**继续：

1. **嫌疑 span 的 service + 时间窗 → Loki 拉错误日志**
   - 走 routing `observability.loki.logql_selector_chain`,优先用 wizard 配过的 `(env_label, service_label)` 命中
   - 时间范围：trace start ± 30s，避免遗漏前后关联日志
{{- if hasSkill . "k8s-runtime-query" }}
2. **嫌疑 span 的 service + 时间窗 → KuboardGetPodLogs / KuboardListEvents**
   - 该 service 对应的 K8s workload(routing `observability.k8s_runtime.service_map[env+service]` 拿 cluster/namespace/label_selector)
   - 看 trace 时间点附近有没有 OOMKilled / CrashLoopBackOff / 突发重启
{{- end}}
{{- if .Infrastructure.Observability.Prometheus.Enabled }}
3. **嫌疑 span 的 service + 时间窗 → PromQL 同窗口指标**
   - 走 routing `observability.prometheus.selector_chain` 拼 selector
   - 关键指标：`rate(http_requests_total{...}[1m])` 看 QPS / `histogram_quantile(0.99, ...)` 看 p99 / 错误率 突变
{{- end}}
4. **该 service 最近 1h 变更**
   - 同窗口 K8s `kubectl rollout history deployment/<name>`(看新 RS 是否刚上)
   - 配置中心 history(看是否最近改了下游地址 / 超时阈值 / 限流参数)
   - git log(看是否最近合并)

**反模式**:trace 看完就停在"OK 是 X 服务慢"。这只是症状,根因是 X 服务的 pod / 配置 / 代码哪个出了问题,必须再走一步上面的协作链才能给出可处置的建议。

## 硬约束

- 仅只读查询；不修改 trace 数据。
- 必须先通过 `routing` 确认 env，再用对应 env 的 Jaeger URL。
- 展示 span 详情时对敏感 tag（token、password）做脱敏。

## 自检示例

**Q1（有 trace_id）**：`trace_id=abc123 这条链路最慢的 span 是哪个？`
- 期望：调 `<JAEGER_URL>/api/traces/abc123` → 解析 spans，按 duration 排序 → TOP 3 span 的 `service + operation + duration`，结尾说清楚 trace 覆盖的时间段和总耗时。

**Q2（无 trace_id）**：`prod 的 order-service 创建订单接口最近 5 分钟 p99 怎么样？`
- 期望：按 service + time range 搜 trace（`/api/traces?service=order-service&start=...&end=...`）→ 统计 p99，附命中 trace 数；若样本 < 10 提示采样率低。

**Q3（trace 找不到）**：用户给的 trace_id 不存在。
- 期望：按 AGENTS.md "找不到 trace / span" 段回复（采样率 / 时间窗 / trace 头传递是否断在 gateway），不要只说"没找到"。
